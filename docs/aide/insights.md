<!--
  AIDE insight inbox. Any role appends ONE line here, at any time, when it
  learns something true but out of scope for its task, and returns to that
  task. The engine creates docs/aide/insights.md from this file, byte for
  byte, the first time `aide check`, `claim`, `queue start` or `insights
  list` finds it missing; nobody copies it by hand.
  The rules — what is captured, what is immutable, what each verb does — are
  conventions.md §1 → insights.md and `python .aide/scripts/aide.py insights
  -h`. This comment is the shapes; the aide-template line below it names
  the template version this inbox was created from.

  Entry:
    - [ ] <type> — <one line> *(<provenance>, YYYY-MM-DD, engine X.Y.Z)*
  The date is required; the provenance before it and the engine version
  after it are free-form and optional. Conventional provenance spellings:
    *(item 099, 2026-07-26)*           one item
    *(items 099-101, 2026-07-27)*      a finding that spans several
    *(queue-014, 2026-07-26)*          planning or spec-authoring, before any item
    *(2026-07-26)*                     no item or queue to name
    *(item 099, 2026-07-26, engine 1.22.0)*   with the engine it was observed under
  Types:
    knowledge  — true fact worth documenting
    defect     — something is wrong and needs a fix item
    gap        — something is missing and needs planning
    automation — a recurring manual/agent action that code could replace
    framework  — belongs to AIDE itself, not this project

  A triaged entry, ticked in place with a pointer:
    - [x] <type> — <one line> *(item NNN, YYYY-MM-DD, engine X.Y.Z)* → <where it landed>
  A status trail, appended under an entry after triage, newest last:
    - [x] framework — <the original claim, never touched> *(2026-08-20)*
      - **2026-08-20** → aide-loop issue #50
      - **2026-10-11** → resolved in engine 1.16.0
-->
<!-- aide-template: insights 1 -->
# Insight Inbox

_Entries below, newest last._

- [x] defect — `aide merge` (auto-merge mode) merged item 135 to `aide/queue-018`, pushed it to origin, and set the item to done (commit `cff1db2`) even though its own re-run post-merge test suite came back red — it printed "the post-merge test run FAILED — investigate" but did not abort, roll back, or block the status flip. Root cause of the one failing test (`tests/test_135_stage29_validation.py::test_ac27_aide_check_reports_no_error_and_only_baseline_warning_classes`): the merged item's own local claim branch `aide/135-validate-stage-29-golden-retirement` was still present when the post-merge `aide check` ran, so `aide check` emitted a new "stale claim branch ... item 135 is already ✅" warning that AC27's baseline warning-class list does not recognise — a self-referential edge case where an item's post-merge validation trips over its own not-yet-deleted claim branch. The shared branch `aide/queue-018` is now red on origin and item 135 is marked ✅ despite the failure; needs a human decision on whether to (a) have `aide merge` delete/detach the claim branch before its post-merge test run, (b) have it treat a FAILED post-merge run as blocking rather than advisory, or (c) both *(item 135, validation round 3, 2026-09-01)* → aide-loop#125
  - **2026-09-01** (pending handover) → aide-loop issue drafted at the 2026-09-01 feedback-loop triage, not yet filed — filing needs a human to confirm; the draft names engine 1.21.0 as the observed version
  - **2026-09-02** → resolved upstream in engine 1.31.0, landed here with the 1.34.0 install, taking option **(c)** — both remedies this entry offered. A red post-merge test run is now a gate rather than a report: green earns the ✅ and the push, red leaves the merge local, the item 🔍 and the claim branch restored. And the claim branch is deleted **before** the run rather than after, so the run sees the refs a fresh clone would. The upstream entry names this repo's exact failure class as its reason: with the branch present, a test command including `aide check` reported the item's own branch as stale against the item being merged — "a failure class produced by nothing but the ordering".

- [x] defect — nine pre-existing test files call `subprocess.run(..., text=True)` without `encoding=`, so captured output decodes with the platform locale: UTF-8 on the Linux CI runners but the ANSI code page (cp1252 by default) on Windows, where any non-ASCII byte in the capture becomes mojibake. The sites: `tests/conftest.py` (2), `test_066_dockerfile.py`, `test_069_container_smoke.py`, `test_070_acceptance_stage9.py`, `test_074_benchmark.py`, `test_111_golden_guard.py`, `test_113_ci_numpy_matrix_scope.py`, `test_117_scope_verb_swap.py`, `test_123_recalibrate_and_regenerate.py` (2). All capture ASCII-only output today (docker/git status lines, filenames), which is why none has fired — but the identical pattern written by queue-018's items broke `tests/test_134_decision_table_evidence_companion.py::test_ac10` on `windows-latest` (PR #58) the first time a capture carried an em dash. `tests/run_process.py::run_utf8` (added on `aide/queue-018`) is the drop-in replacement; converting these nine files to it closes the class in the suite *(queue-018, 2026-09-01)* → roadmap.md Backlog — 'Nine pre-existing test files capture subprocess output with text=True and no encoding=' (2026-09-01 triage)

- [x] framework — `.aide/conventions.md` §6 (test hygiene) has no rule that a test capturing subprocess output with `text=True` must pass `encoding="utf-8"`, and six queue-018 items (126, 127, 128, 132, 134, 135) each independently wrote the unencoded form; the Linux-only validator passed all of them because `locale.getpreferredencoding` is UTF-8 there, and the defect surfaced only on `windows-latest` CI (PR #58: `KeyError` on a cp1252-decoded em-dash heading in `test_134`'s AC10 section lookup — beyond the crash, `test_135`'s AC26 emoji-diff guard was silently defeated the same way, a false negative rather than a failure). §6 already names the neighbouring classes (`.as_posix()`, `.gitattributes` eol pins) and `aide check` lints the decidable ones; this class is equally decidable by AST — a `subprocess.run` call carrying a `text`/`universal_newlines` keyword and no `encoding` keyword — so it warrants both the §6 rule and an `aide check` lint alongside the eol-pin one *(queue-018, 2026-09-01)* → aide-loop#126
  - **2026-09-01** (pending handover) → aide-loop issue drafted at the 2026-09-01 feedback-loop triage, not yet filed — filing needs a human to confirm; the draft names engine 1.21.0 as the observed version
  - **2026-09-01** → resolved upstream in engine 1.29.0: §6 states the rule and `aide check`'s `subprocess_encoding_test_warnings` decides it by AST. Landed here with the 1.29.3 install, which flagged 11 call sites across 9 files (`tests/conftest.py`, `test_066`, `test_069`, `test_070`, `test_074`, `test_111`, `test_113`, `test_117`, `test_123`), all fixed in the same change. 1.29.0 also fixed the same shape inside the engine's own `git()`, `aide env` and `aide status`; 1.29.3 fixed it in the command-hygiene hook's own stderr

- [x] gap — `tests/test_115_stage26_validation.py::_all_sha256_findings` re-walks every `tests/*.py` (154 files) on each call and takes **49 s**, and the suite pays it four times: `test_ac8_no_hardcoded_literal_fence_remains` and `test_ac8_intra_run_digest_assertions_still_exist` once each, plus `tests/test_128_relocation_checks.py::test_ac13_test115_fence_cap_still_passes_and_points_at_new_module` twice (it calls the AC8 test *and* `_all_sha256_findings` itself, hence its 103 s) — ~4.1 min of the serial suite, and the largest single test in the run, so it also sets the floor `pytest -n 4` can reach. cProfile, 2026-09-01: 94% of the time is `_source_segment` -> `ast.get_source_segment` -> `_splitlines_no_ff`, which re-splits the whole file source on every call, once per candidate comparison side inside `_computes_sha256` (20.5 M `len` calls on one 2.5 s file). Two independent fixes, neither changing what is asserted: split each file's source once per `_classify_sha256_compares` call and reuse the lines, and memoise `_all_sha256_findings` at module level so four sweeps become one. Left alone deliberately in the CI wall-clock PR (branch `ci/xdist-and-pip-cache`), which changes no test semantics *(2026-09-01, engine 1.28.1)* → tests/test_115_stage26_validation.py, branch perf/115-sha256-sweep-source-split — per-call source split hoisted, 53.4 s to 2.4 s a sweep, suite 293 s to 214 s under -n 4; memoisation not taken (xdist workers are separate processes)

- [ ] gap — three of the five CI legs in `.github/workflows/ci.yml` install `pip install -e .[dev]` with no `-c constraints.txt` (both `test-numpy-majors` legs and `verify-environment-gated`), so every dev-extra tool floats there while the `test` job resolves the lockfile's pins — and `pyproject.toml` bounds that tooling only from below (`pytest>=7`, `pytest-xdist>=3`), so a future pytest 9 or xdist 4 lands on exactly those three legs first, with the pinned `test` job staying green and giving no warning. Neither leg can simply adopt `-c constraints.txt`: it pins `numpy==2.4.6`, which is the pin the numpy legs exist to override and which would fight pyradiomics's own floor in the gated job. Raised by the Copilot review of PR #60 against `pytest-xdist` specifically (2026-09-01); the version-specific fix it proposed — hardcoding `pytest-xdist==3.8.0` into two more workflow steps — was declined as a third unchecked copy of one version, the failure mode `pyproject.toml` + `constraints.txt` + five test literals already has for tptbox. The general answer is a dev-tooling lockfile separate from the image's runtime graph (a `constraints-dev.txt` applied on every leg, with the numpy override applied after), which is a packaging decision, not a CI wall-clock one *(2026-09-01, engine 1.28.1)*

- [ ] gap — `segfacet evaluate --calibrate` re-extracts every case's features once per grid point. `segfacet.eval.calibrate.calibrate_thresholds` re-runs `evaluate_cohort` at each setting in the Cartesian product, and `segfacet.eval.harness.evaluate_case` then reloads both label maps, runs `run_qc` (full feature extraction + rules), a full-array `compute_overlap`, and a *second* `extract_feature_record(gt_img)` for the feature match — none of which depends on the swept values, because a `ThresholdAxis` only ever writes into a rule's `params` block, which is consumed downstream in `heuristics.run_rules`. `default_calibration_axes()` is 5x5x5 = 125 points, so a default sweep repeats all of that 125 times over. Measured 2026-09-01 on the 2-case cohort in `tests/test_057_evaluate_cli.py`: the same CLI invocation costs 0.64 s without `--calibrate` and 74.25 s with it. Hoisting the per-case feature record out of the grid loop (extract once, re-run only the rules per setting) would cut a real calibration run by roughly the grid size. Separately, `segfacet.cli`'s `_handle_evaluate` hardcodes `default_calibration_axes()` with no flag to bound the grid, so a real cohort cannot ask for a cheaper sweep at all. Both are production changes needing an item; the *test*-side cost was addressed independently by shrinking the axes inside that one test *(2026-09-01, engine 1.28.1)*

- [x] framework — engine 1.35.0's `aide progress reword` guard against an annotated criterion is bypassed by a `)` anywhere in the annotation. `_ACCEPT_EVIDENCE_RE = re.compile(r"\*\([^)]*\)\*\s*$")` cannot cross the first `)`, so a box ending `*(measured on CPU (4 cores), 2026-09-02)*` matches nothing and `reword_criterion` treats it as unevidenced — then rewrites the whole box body, **deleting the annotation** rather than merely re-pointing it, which is the one thing the verb's docstring says it exists to prevent. Reproduced against the installed 1.35.0 with a three-line `progress.md`: refused with a paren-free annotation, reworded with a parenthesised one. Reachability is narrower than the regex suggests and that is luck, not design — the two sibling guards catch the states the CLI can produce (`accept --evidence` ticks, so the tick guard fires first; `retract` unticks but writes a trail, so the trail guard fires) — leaving an unticked, trail-free, annotated box, which today needs a hand edit to reach. Evidence text carrying parentheses is ordinary, so the guard should hold on its own: match the last `*(`…`)*` rather than the first, or read the annotation the way `accept` writes it. Raised by the Copilot review of this repo's PR #66 (`.aide/scripts/aide.py:850`, 2026-09-02) *(2026-09-02, engine 1.35.0)* → aide-loop#143
  - **2026-09-02** → fixed upstream in engine 1.35.1, one release after the report: `_ACCEPT_EVIDENCE_RE`'s body is now greedy with the close anchored at end of line, so `reword` sees an annotation carrying its own parentheses and refuses. Landed here with the 1.37.0 install. The upstream entry agrees on reachability — no live path reached it, since the tick and trail guards cover every state the CLI can produce — and fixes it on the stated ground that a precondition named as one of three independent checks has to be one.

- [x] gap — a stage superseded in part records the supersession only in the superseding stage, so reading the superseded stage top-down in `roadmap.md` yields the stale plan. Roadmap Stage 20 D4 still reads "mode 1's ladder is FOV-capped, mode 4's cause is to be determined by the stage" and still proposes the FOV-headroom remedy, though Stage 28 resolved both modes as one defect (the interpolating spline fit) and items 120/132/135 closed them; `roadmap.md` states that only inside its own Stage 28 section, ~340 lines further down. `progress.md`'s mirror of the same deliverable does carry an inline "⚠️ Superseded in part, 2026-08-27" note, so the two documents disagree about how discoverable the supersession is, and the roadmap — the document a stage is scoped from — is the one without it. Found while scoping queue-019 against Stage 20, where three of the goal paragraph's numbers had also moved (4 of 10 rules firing is now 5, 3 of 9 silent cases is now 1, cross-talk 0/9 is now 1/9). Backward supersession markers are a root-document edit, so they need the create-roadmap entry point and a reviewed PR, not an item *(queue-019, 2026-09-02, engine 1.37.0)* → roadmap.md v3, commit b0c645e (PR #71) — the backward supersession record now heads Stage 20, not an item

- [x] knowledge — the "72 of 111 statused-but-mode-unmapped" figure that `roadmap.md`'s 2026-08-11 G8 amendment and `progress.md`'s Stage 19 attestation both cite is stale against the artifact it was measured on. `docs/aide/feature_catalogue.generated.json` now holds **138** entries whose `mode_evidence` splits **86 empty · 32 `("rule_mode_map",)`-bearing · 18 `("rule_unmapped",)` · 2 `("per_mode_metric",)`** (measured 2026-09-02 while specifying item 136). The mode-unmapped bucket shrank because items 110/120/124/131/132 added leaf paths and moved attributions, **not** because anything dispositioned the four mode-less rules — the shortfall is unchanged and is still items 136/137's to close. Stage 20's honest-count item (142) should quote the re-measured split rather than the attestation's numbers, and the attestation itself is immutable, so any correction there is an `aide progress amend` append, never a rewrite *(item 136, 2026-09-02, engine 1.37.0)* → progress.md Stage 19 criterion 2, dated amendment 2026-09-03 (re-measured split: 138 entries, 0 rule_unmapped)

- [x] defect — the `"corpus"` evidence tag that gates `catalogue.rule_declaration_conflicts()`'s declaration-to-corpus direction is an exact-element membership test (`if "corpus" in decl.evidence`) over a free-form tuple that nothing validates, so a near-miss silently disables the check rather than failing. A declaration written `evidence=("corpus:CropAtBorderPerturbation",)` reads as corpus-backed to a human, is well-formed under `RuleModeDeclaration.__post_init__`, and never enters the branch — a declared mode no corpus case supports is then reported as agreement. The guard reports a clean tree while checking nothing, the recurring defect class `REVIEW.md` names first. Either anchor the check on a `"corpus"` prefix or reject an evidence element that contains but does not equal the reserved tag. Found by the rank-3 per-item review of item 136 (`src/segfacet/catalogue.py:1046`, `src/segfacet/heuristics/rule.py:96-119`); `rule.py` is outside every queue-019 item's authorised paths *(item 136, 2026-09-02, engine 1.37.0)* → item 147 (queue-020)

- [x] defect — `RuleModeDeclaration.__post_init__` validates the elements of `evidence` but never that `evidence` and `modes` are tuples, so a bare string and a list both construct. `evidence="corpus"` (parentheses forgotten) passes, iterates one character per element, and binds the reserved tag by substring accident; `evidence="corpus-derived"` would bind it the same way while rendering one character per cell in item 138's matrix. A list-valued `modes` on a registered rule stays mutable in place, which item 136's `test_adv_live_declaration_object_cannot_be_mutated_in_place` does not catch because it only tests rebinding. Separately, `modes=3` raises `TypeError`, not the `ValueError` naming the offending field that AC2 promises. Found by the rank-3 per-item review of item 136 (`src/segfacet/heuristics/rule.py:113-118`) *(item 136, 2026-09-02, engine 1.37.0)* → item 147 (queue-020)

- [x] gap — `catalogue.rule_declaration_conflicts()`'s corpus-to-declaration direction iterates the registered rules, so a corpus case designating a `rule_id` that no rule registers is never consulted and never reported. An `Expectation(failure_mode=3, expected_rule_ids=frozenset({"boundary"}))` — a typo for `"border"` — yields no message. Item 136's own suite catches it by a separate route, but items 138/139/142 are pointed at `rule_declaration_conflicts()` as *the* agreement gate, and it is blind to this direction. Found by the rank-3 per-item review of item 136 (`src/segfacet/catalogue.py:1027-1035`) *(item 136, 2026-09-02, engine 1.37.0)* → item 147 (queue-020)

- [x] gap — §6's eight failure modes (`vision.md` "Segmentation Failure Modes") are all geometric/topological/semantic and name no *tissue-plausibility* mode, so the `intensity` and `intensity_reference_delta` rules — which threshold a labelled region's HU distribution for implausibly-low (soft tissue/air), implausibly-high (metal/implant) or near-zero-std (degenerate/uniform) statistics — are demonstrably useful, demonstrably exercised, and legitimately mode-less rather than mis-mapped or speculative. Corroboration: `tests/corpus/intensity/manifest.json`'s four cases (`clean_hu`, `implausible_metal`, `implausible_soft_tissue`, `degenerate_uniform`) drive them, and that manifest carries no `failure_mode` field at all — the same gap, expressed in the corpus. Closing it needs a ninth §6 mode, which is a `vision.md` edit through `/aide-create-vision` and a reviewed PR, not an item; item 137 disposed both rules mode-less with this reason recorded on their own `RuleModeDeclaration`s rather than growing §6 from inside an item *(item 137, 2026-09-02, engine 1.37.0)* → item 146 (queue-020) — the ninth mode, entered through the lifecycle
- [ ] gap — acceptance criterion retracted: Retracting an attestation-mapping error: this criterion (end-to-end detection count stated honestly) was mistakenly ticked using item 137's AC5 test as evidence by positional mismapping; that test verifies mode-less declarations, not this stage-level claim, which remains open (Item 142). *(stage 20 criterion 5, 2026-09-02, engine 1.37.0)*
- [ ] gap — acceptance criterion retracted: Retracting an attestation-mapping error: this criterion (specificity assertion enforced for every corpus case) was mistakenly ticked by positional mismapping to item 137's AC4 test; the specificity assertion is Item 140's future deliverable, not yet built, so this criterion does not hold. *(stage 20 criterion 4, 2026-09-02, engine 1.37.0)*
- [ ] gap — acceptance criterion retracted: Retracting an attestation-mapping error: this criterion (every registered rule exercised by >=1 case or recorded unexercised with reason) was mistakenly ticked by positional mismapping to item 137's AC3 test; per-rule corpus-exercise reporting is Item 139's future deliverable, not verified by item 137's tests. *(stage 20 criterion 3, 2026-09-02, engine 1.37.0)*
- [ ] gap — acceptance criterion retracted: Retracting an attestation-mapping error: this criterion (every mode has >=1 rule and a recorded evidence rung) requires the mode->rule direction with evidence rungs, which is Item 138's traceability-matrix deliverable (still open, marked pending in this stage's Deliverables); item 137 only disposed the four previously-undeclared rules, it did not build the evidence-rung matrix. *(stage 20 criterion 1, 2026-09-02, engine 1.37.0)*

- [x] framework — `aide progress retract` turns a withdrawn attestation into a **permanent** `aide check` warning by deliberate design (`.aide/scripts/aide.py:3382-3390`, "A withdrawn attestation is normal, not a defect... the point is that it stays visible"), so any consumer test that pins the set of tolerated `aide check` warnings goes red the first time anyone retracts anything, and stays red forever. Hit live during item 137: four Stage 20 retractions turned the suite red in `tests/test_114_documentation_corrections.py::test_ac8_no_new_aide_check_warning_beyond_pinned_baseline` and `tests/test_135_stage29_validation.py::test_ac27_aide_check_reports_no_error_and_only_baseline_warning_classes` — neither of which had ever been exercised against a retraction — and blocked the merge gate on a consequence of honest self-correction rather than on any defect in the item. The engine is right that the record must stay visible; the cost is that retracting is a suite-reddening act in any consumer that pins warning output, which nothing warns you about at retraction time. Either the warning wants a distinct non-baseline channel, or `retract`'s output should say that a pinned-baseline consumer will now need widening *(item 137, 2026-09-02, engine 1.37.0)* → aide-loop#152

- [x] gap — `tests/committed_artifact_guard.py`'s closed `GROUNDS` vocabulary has no member describing a **float-free** derived artifact, so the one artifact shape whose byte-exact fresh-vs-committed comparison is unconditionally safe is the shape that cannot be allowlisted. The five grounds all describe *why the floats are stable* (`exact-parameter-floats`, `emission-clamped`, `hand-written-literals`, `binary-fixture`, `integrity-pin`); an artifact of derived strings and integer counts matches none of them honestly, and `tests/test_134_decision_table_evidence_companion.py::test_ac16_committed_artifact_guard_clean_and_vocabulary_unextended` pins the vocabulary at exactly five members. Item 134 routed around it by comparing `json.loads` payloads (a shape the classifier skips in silence) and item 138's spec does the same for the same reason — two items now taking a documented dodge rather than declaring a ground, which is the state a closed vocabulary is meant to prevent. A sixth ground (`no-float-leaf`, discharged by the artifact's own "no float leaf" test) would let such an artifact make the stronger byte-exact claim under the guard rather than beside it *(item 138, 2026-09-02, engine 1.37.0)* → item 149 (queue-020) — the sixth GROUNDS member, no-float-leaf

- [x] gap — a `RuleModeDeclaration.evidence` string asserting a fact about a committed artifact shipped false and went undetected because item 137's AC4 test only checked `len(evidence_sentence) >= 40` — a length floor, not a content check. `reference_delta`'s evidence claimed "the only per-label feature the committed reference artifact carries is physical_volume_mm3", but both committed reference artifacts carry 21 per-label features and `compute_reference_delta` scores every one it finds a case value for, including `spline_offset_mm` (§6 mode 1's own anchor) — so the rule under-declared its modes until a post-merge review measured the artifacts directly and corrected it (item 137's Decisions log, "Correction (2026-09-02, post-merge review)"). A declaration that asserts a fact about code or a committed artifact needs that fact actually measured in the same change that writes the sentence, not just a length check on the prose. *(item 137, 2026-09-02, engine 1.37.0)* → items 145, 149 (queue-020) — expected firing sets measured, then scored against measured

- [x] gap — item 136's §6 mode attribution is **rule-granular**: every leaf path a declaring rule consumes gains that rule's full mode tuple, so the generated catalogue paints pure bookkeeping paths with failure modes they cannot evidence — after commit `b1c593c`, `reference_delta.lower_pct`, `reference_delta.{label}.label` and `reference_delta.{label}.level_name` each carry `failure_modes == (1, 2)`, and a reader of `docs/aide/feature_catalogue.generated.md` cannot tell those apart from `reference_delta.{label}.features.physical_volume_mm3.robust_z`, which genuinely carries the mode-2 signal. Anything computing a per-mode feature set from `consuming_rules` inherits it — item 138's mode → feature direction does, and reports the granularity beside the list (its A13/AC33) rather than filtering, because narrowing would need a per-path mechanism claim no shipped `RuleModeDeclaration` carries. The fix belongs in the declaration seam: a per-path or per-detector mode attribution, or an explicit "bookkeeping" classification the catalogue can exclude from mode columns *(item 138, 2026-09-02, engine 1.37.0)* → item 148 (queue-020) — per-detector attribution

- [x] gap — item 138's AC31 token-presence check (a mechanism sentence must name a token that resolves against live state: an anchor path, a corpus `case_id`, or a `rule_id`) passed on all eight authored `MODE_RUNGS` mechanisms while four were factually false, because the token it accepts is never the constraint that would catch the error: for modes 4 and 5 the false claim named the very anchor path `feature_docs.py` documents as deliberately *not* what the rule reads (`stage3.monotonic_consistency.is_monotonic` vs the rule's actual `non_monotonic_pairs[]`; `relationships.present_levels[]` vs the rule's actual `missing_levels[]`), and for modes 1 and 2 the claim was about which rules fire together, a relationship no single resolvable token constrains at all. A token-presence check on a factual sentence proves the sentence mentions something real, never that what it says about that thing is true — the same failure shape as item 137's `len(evidence) >= 40` floor (`insights.md`, 2026-09-02, "a length floor, not a content check"), one level up: a resolvable-token floor is a content-shape check, not a content-truth check. Found and fixed by a post-merge code review of item 138 (`src/segfacet/traceability.py`'s `MODE_RUNGS`, modes 1/2/4/5/6 — mode 6 was a fifth, simpler defect: the corpus operator crops the anterior face, not the left face); the fix is recorded in item 138's Decisions log, "Correction (2026-09-03, post-merge code review)" *(item 138, 2026-09-03, engine 1.37.0)* → items 145, 147 (queue-020) — MODE_RUNGS retired for authored per-edge rungs

- [x] automation — `tests/test_138_traceability_matrix.py` calls `segfacet.traceability.build_matrix()` 42 times with no shared fixture, so every future extension of that one generator multiplies across the whole module. Item 139 adds the corpus-exercise directions to the same builder, taking it from ~0.6 s to ~3.1 s per call (measured 2026-09-02: +1.7 s for nine geometric cases plus one reconstruction, +0.8 s for four intensity cases, +0.01 s for the `fuse` probe), which grows that module by roughly 105 s on its own. A module-scoped fixture would recover nearly all of it, but the tests deliberately monkeypatch the registry, the manifests and `MODE_RUNGS` and require re-derivation, so the fix is a fixture per patch-group rather than one shared matrix — and a module-level cache in the generator would silently defeat exactly the adversarial tests that prove the report is live. A shared expensive builder needs its call-count discipline set when the first consumer module is written, not after the third extension *(item 139, 2026-09-03, engine 1.37.0)* → item 149 (queue-020) — build_matrix fixture discipline
- [x] gap — `segfacet.synth.regression` is documented as the importable verification library over the committed corpus, but it covers only the geometric corpus (`tests/corpus/manifest.json`): there is no intensity sibling of `pipeline_findings`, so anything wanting to drive `tests/corpus/intensity/manifest.json`'s four cases must re-compose `io.load_case` + `pipeline.run_qc_with_intensity(..., reference=None)` itself. Item 139 does exactly that, privately inside `src/segfacet/traceability.py`, rather than adding the sibling — because item 140 edits `verify_case` in that same file in the same queue and the collision was not worth the tidier home. The second committed corpus has had no harness of its own since item 058 shipped it; the duplication becomes real the moment a second consumer wants it *(item 139, 2026-09-03, engine 1.37.0)* → item 146 (queue-020) — the intensity sibling built in synth/regression.py
- [x] framework — a validator ticked four stage acceptance criteria by **positional index**: item 137's validator mapped the item's ACs 1–5 onto Stage 20's criteria 1–5 by number and ran `aide progress accept 20 --criterion N` for each, so criteria 1, 3, 4 and 5 were attested against tests that verified something else (mode-less declarations, not the evidence-rung matrix, the exercise report, the specificity assertion or the honest count), and all four had to be retracted the same day. `validator.md` step 2 says "tick only what you verified", but nothing in the spec template or the validator prose ties an item AC to a *named* stage criterion, so the only available mapping is the index, and an index coincidence reads as verification. The fix is structural: an item spec names the stage criterion each AC closes (or none), and the validator ticks a stage criterion only when the spec names it — never by position *(item 137, 2026-09-03, engine 1.37.0)* → aide-loop#153
- [x] framework — an acceptance criterion that asserts a **fact about code or a committed artifact** was accepted on a *shape* check three times in one queue: a length floor (`len(evidence) >= 40`, item 137), a resolvable-token presence check (item 138's AC31) and a completeness flag derived from declaration state rather than from the thing declared (item 138's `rule_to_mode: complete`). Each passed a false factual sentence, each was found only by a post-merge human review, and each fix produced a stronger check that the next false claim still bypassed. The spec-author and test-writer prose say nothing about this class: neither forbids an AC whose test proves the claim *mentions* something real rather than that what it says is true. The authoring-time rule wanted is that an AC asserting a fact about live state must be worded as a **measured equality against that state** (the artifact's actual field set, the pipeline's actual firing set), and the test-writer must refuse a test that checks only a sentence's length, tokens or presence. `REVIEW.md` in this repo names the class for reviewers; the loop needs it at authoring time *(items 137–138, 2026-09-03, engine 1.37.0)* → aide-loop#154
- [x] framework — `rollup_status` (`.aide/scripts/aide.py:502-516`) treats a ⏸️ **deliverable** as terminal alongside ✅ and ❌, so a stage paused mid-flight rolls up to `complete` and `aide check` emits "stage N: all deliverables ✅ but summary shows in-progress — … close the stage", advising exactly the wrong action. Hit live at the queue-020 boundary: Stage 20's four remaining deliverables (items 139–142) were flipped 📋 → ⏸️ because their prose had said Deferred since 2026-09-03 while their icon still said Planned, which left queue-019 derived-open and `aide claim --dry-run` offering item 139 — the very item held pending Stage 30's sign-off. Both readings cannot be had at once today: 📋 keeps the stage honestly 🚧 but hands held work to `claim`, and ⏸️ stops `claim` but declares the stage shipped. The engine already draws the distinction it needs one layer down — `aide scope`'s spent set is `{complete, excluded}` and its comment says "Deferred (⏸️) items are NOT spent: their claims are dormant, not dead" (`aide.py:3765-3778`) — so `rollup_status` disagreeing with `scope` about what ⏸️ means is the defect, not the icon choice. A stage whose only non-✅ deliverables are ⏸️ is `in-progress`, or wants a status of its own *(queue-020, 2026-09-03, engine 1.37.0)* → fixed upstream in engine 1.41.0 (aide-loop#173), installed 2026-09-16 with engine 1.52.1; Stage 20 summary row and heading restored to 🚧 in the same change
- [x] defect — `aide progress set 143 in-progress` (commit `5f5f103`, inside item 143's slice) rewrote `docs/aide/progress.md`'s Stage 20 summary row **and** its section heading from 🚧 to ✅, contradicting three still-unticked Stage 20 acceptance criteria and the stage's own queue-020 boundary note twelve lines above them ("Do not act on that advice — Stage 20 is not complete, and its summary row stays 🚧"). The flip also silenced the `aide check` warning that same note declared part of the warning baseline from 2026-09-03, so the baseline moved with nothing announcing it. This is the committed consequence of the `rollup_status` ⏸️-is-terminal defect captured in the queue-020, 2026-09-03 entry above: that entry describes the mechanism, this one records that a status verb has now written the wrong stage status into the single source of truth, where the queue planner and item 151 will read it. Needs a human to restore 🚧 *(item 143, 2026-09-03, engine 1.37.0)* → Stage 20 summary row and section heading restored to 🚧 in the engine 1.52.1 update change (2026-09-16), under which a ⏸️ deliverable no longer rolls its stage up (engine 1.41.0)
- [x] gap — Stage 30 acceptance criterion 7 was ticked by item 143's validator (`docs/aide/progress.md`, commit `5a663d0`) although queue-020 assigns the ticking of Stage 30's seven criteria to item 151, the stage-validation item. Its first two clauses are true as measured (the corpus advances caudally; every committed corpus value and both reference artifacts were regenerated after the correction). Its third — "no expected firing set in the specification predates it" — is not establishable by item 143 at all, because the specification module it refers to is item 144's deliverable and does not exist yet; the recorded evidence sentence argues the point rather than measuring it. Item 151 must re-verify criterion 7 against the built specification and not read the existing tick as settled *(item 143, 2026-09-03, engine 1.37.0)* → item 151: re-verified against the built specification (src/segfacet/failure_modes.py, SPECIFICATION) in a clean clone; AC8/AC30-AC34 confirm the third clause independently. progress.md Stage 30 criterion 7 amended.
- [x] gap — `docs/aide/traceability_matrix.generated.{json,md}` is now byte-compared fresh-vs-committed (`tests/test_143_s_axis_correction.py`, AC15) with no entry in `tests/committed_artifact_guard.ALLOWLIST` naming a ground for it. `classify_module` returns clean only because it cannot resolve a committed path reached through a two-hop `_TESTS_DIR` -> `_REPO_ROOT` module root, one of the shapes its own docstring says it skips in silence — so `test_ac14_this_module_produces_no_guard_violation` passes without ever seeing the comparison. The comparison is sound on its merits (the artifact has zero float leaves, measured 2026-09-03), which is exactly why it deserves an explicit allowlist entry rather than a blind spot; item 143's AC14 forbade allowlist growth, so the justification currently lives in a source comment *(item 143, 2026-09-03, engine 1.37.0)* → item 158
- [x] defect — `tests/test_143_s_axis_correction.py::test_ac16_record_covers_exactly_the_required_artifact_set` asserts that `docs/corpus-s-axis-correction.md`'s table rows **equal** a set derived live from both corpus manifests' `scan_fixture`/`seg_fixture` paths plus eight named artifacts. The record is a dated historical account of one item's regeneration, but the equality binds it to a growing input: the first later item that adds a corpus case or fixture turns that record red and forces a retro-edit of a document about 2026-09-03. A subset assertion over the artifacts that existed when the record was written, or a frozen path list captured in the test, would keep the coverage claim without pinning history to live state *(item 143, 2026-09-03, engine 1.37.0)* → item 159
- [x] defect — `tests/test_128_relocation_checks.py:748` and `tests/test_135_stage29_validation.py:852,1005` skip themselves when the recorded `aide/queue-018` base ref is unavailable, and that ref no longer exists anywhere in the repository (checked 2026-09-03, no local or remote-tracking branch matches) — so all three have already degraded to permanent silent skips, the repo's number-one recurring defect class (`REVIEW.md`, "What Important means here"). Found while running the full suite for item 143's review; unrelated to that item's change *(item 143, 2026-09-03, engine 1.37.0)* → commit b11cef3: all three retired from the suite per conventions §6 (a diff-scope claim belongs on the branch); test_135's synthetic AC26/AC28 companions removed with them
- [x] defect — `src/segfacet/__init__.py` unconditionally imports `segfacet.features.fragmentation` (`compute_fragmentation_index`), which imports `nibabel` at module level, so any `import segfacet.X` — including `traceability.py`, whose own docstring claims to stay import-cheap — transitively loads NumPy/NiBabel via the parent package init before the submodule body ever runs; confirmed live for both `segfacet.traceability` and the new `segfacet.failure_modes` (item 144), whose own AC1 sub-test asserting a bare import adds no heavy module to `sys.modules` cannot pass without deferring this one `__init__.py` import, a path outside item 144's authorised paths *(item 144, 2026-09-03, engine 1.37.0)* → item 159
- [x] gap — `tests/committed_artifact_guard.py`'s classifier resolves a module root only from a `Path(__file__)` chain of at least two literal `.parent` steps (`_is_file_root_chain`, `committed_artifact_guard.py:239-255`), so the equally common `Path(__file__).resolve().parents[1]` idiom resolves to nothing and every committed path built from such a root is skipped in silence — measured 2026-09-03: the same two-line module classifies clean written with `parents[1]` and reports a `Violation` written with `.parent.parent`. Both `tests/test_138_traceability_matrix.py:69` and `tests/test_144_failure_mode_specification.py:33` use `parents[1]`, which makes test_138's `test_ac29_committed_artifact_guard_clean_and_grounds_unextended` an attestation over a blind spot; neither module byte-compares fresh-vs-committed today, so nothing is hidden yet, but item 149 plans to move both generated failure-mode artifacts under the guard on a new `no-float-leaf` ground, and an `ALLOWLIST` entry alone will not make that comparison visible while the root idiom stays `parents[1]`. Same defect class as the two-hop `_TESTS_DIR` -> `_REPO_ROOT` blind spot recorded for item 143 above, a different unresolvable shape *(item 144, 2026-09-03, engine 1.37.0)* → item 158
- [x] defect — `segfacet.failure_modes.derive_status` can return `"validated"` for a mode that no registered rule declares, so a mode reaches the top of the lifecycle without ever passing `"implemented"`: the layering (`failure_modes.py:977-981`) tests `mode.corpus_cases and all(case_agrees(...))` first and only consults `_registry_declares` on the way to `"implemented"`. `vision.md` §6's growth contract requires that "every mode at `implemented` or above has a rule", and its `validated` bullet requires "a corpus case demonstrates detection end-to-end", but neither is enforced — an authored `expected_firing=()` on a case that fires nothing also agrees vacuously and validates. Live and imminent, not hypothetical: item 146's ninth mode is tissue-plausibility, and both `heuristics/intensity.py:134` and `heuristics/intensity_reference_delta.py:131` declare `mode_less_reason`, not `modes=(9,)`, so an agreeing intensity corpus case would report mode 9 as `validated` with no rule declaring it anywhere. Requiring `_registry_declares(mode.id)` before `"validated"` (and a non-empty measured set) is one expression, changes no committed artifact today because all eight §6 modes are declared, and is a lifecycle-semantics call for the maintainer at item 150 rather than a review fix *(item 145, 2026-09-03, engine 1.37.0)* → decided at item 150 (2026-09-14): validated now requires a non-empty agreeing case that fires one of the mode's own intended rules -- segfacet.failure_modes._demonstrates
- [x] defect — the mode-7 rung rationale is inaccurate in both places that now carry it, `src/segfacet/traceability.py:218` (`MODE_RUNGS[7].mechanism`, pre-existing) and the copy item 145 authored into `src/segfacet/failure_modes.py:780` and hence into the committed `docs/aide/failure_modes.generated.{md,json}`. Measured 2026-09-03 from `segfacet.labels`: `CANONICAL_ORDER` inserts `T13` at index 19, so canonical rank equals the label value for the lumbar labels the claim is about (`L1`=20 → rank 20 … `L5`=24 → rank 24) and equals value − 1 only up to `T12` (19 → 18) — "rank(v) == v - 1 under the TPTBox default" is false for exactly the range §6.7's `L1 → T12 → L2 → L5` example uses, and that example is itself a single rank descent (20, 18, 21, 24), not the "two-descent" input both texts call it. `failure_modes.py`'s copy additionally attributes the cap to "the rule", but `heuristics/sequence.py` caps nothing: it emits one finding whenever `relationships.out_of_order_labels` is non-empty (`sequence.py:12-16`, docstring, and the `evaluate` body) — the single-relabel limitation belongs to the fixture generator `SequenceBreakPerturbation` (`synth/identity_ordering_alignment.py:348-411`). Item 145's AC10b only asserts the tokens `rank(v)`, `v - 1` and the four level names are present, so the wording is pinned but never checked. Item 147 collapses `MODE_RUNGS` onto the specification and is the place to settle one correct sentence rather than diverge the two copies now *(item 145, 2026-09-03, engine 1.37.0)* → item 147: SPECIFICATION[7].mechanism
- [ ] knowledge — the 2026-09-03 entry above recording that item 139 "re-composed it privately inside `src/segfacet/traceability.py`" describes item 139's deferred *spec*, not the tree: item 139 was cut from queue-019 and never merged, and `src/segfacet/traceability.py` on `aide/queue-020` contains zero occurrences of `intensity` and never references `run_qc_with_intensity` (verified 2026-09-03). So there is no private intensity composition to re-point onto item 146's new public `synth/regression.py` harness — the gap the entry names is real, but its "a second consumer already exists" corroboration is not. Whoever specs item 149 should read the entry with this correction beside it *(item 146, 2026-09-03, engine 1.37.0)*
- [x] defect — `tests/test_145_eight_hypothesised_modes.py::test_ac23_fresh_matches_committed_structurally_and_carries_all_eight_ids` asserts `committed_ids == set(_EXPECTED_MODE_IDS)` (exactly `{1..8}`) against `docs/aide/failure_modes.generated.json`'s live mode-id set. Item 146's spec authorises only six named `test_145` tests for reconciliation (its "Existing tests to reconcile" list and Authorised-paths block), and this one is not among them, yet item 146 regenerates that committed JSON to carry modes 9 and 10 too — so once item 146's implementation lands, `committed_ids` becomes `{1..10}` and this assertion goes red on a state the item itself deliberately created, not a regression. The equality should be a subset check (`set(_EXPECTED_MODE_IDS) <= committed_ids`) or restated the way the six authorised tests were, but that edit is out of item 146's authorised scope — found while writing item 146's tests, 2026-09-04 *(item 146, 2026-09-04, engine 1.37.0)* → item 159
- [x] defect — `tests/test_146_ninth_mode_and_first_proposed.py::test_ac30_proposed_entry_acquiring_a_declaring_rule_is_reported` ends with `assert fm.specification_conflicts(tuple(fm.iter_modes())) != conflicts`, commented "The shipped tree itself, without the stub, has none". That assertion cannot hold by construction: `specification_conflicts()`'s own default is `tuple(iter_modes())` (`src/segfacet/failure_modes.py`, the `if modes is None` branch), so the two calls are the same call, and passing an explicit mode tuple cannot un-register the stub rule the test just registered — the `proposed`-drift check derives its status from the live registry, not from the modes argument. Measured on `aide/146-the-ninth-mode-enters-through` with modes 9/10 landed: 87 of the module's 88 tests pass and this is the only failure, with both sides of the `!=` printing the identical one-element tuple. The claim the line is reaching for (the shipped tree reports no conflict) is already made by the test's own first assertion, before the stub is registered. Builder was not authorised to edit the test module *(item 146, 2026-09-04, engine 1.37.0)* → item 159
- [x] framework — `aide merge` deletes the claim branch *before* its post-merge test gate, so a merge run killed mid-suite leaves the item merged into its base but the branch gone. Recreating the branch by hand (`git branch <name> <sha>`) does not restore the `branch.<name>.aide-base` config that `aide claim` wrote, and `resolve_base()` then falls back silently to `main` — the re-run of `aide merge 146` fast-forwarded the whole `aide/queue-020` branch onto `main` and pushed it, bypassing the one-reviewed-PR-per-queue policy (recovered 2026-09-04 by force-pushing `origin/main` back to `8de697c` and cherry-picking the done tick onto the queue branch). Two fixes wanted: the cleanup step runs after the test gate, and a merge whose base falls back from a recorded-but-missing config refuses instead of defaulting to `main` *(item 146, 2026-09-04, engine 1.37.0)* → fixed upstream in engine 1.41.0 (aide-loop#174, precursor #167): an interrupted merge restores the claim branch and its base, and a branch with no recorded base is refused instead of defaulting to main; the delete-before-test order is kept by design (aide-loop#125). Installed 2026-09-16 with engine 1.52.1
- [x] defect — `docs/aide/traceability_matrix.generated.{json,md}` now carries mode 9 as a mode row whose `title` is `""`, `pipeline_detected` is `false` and `cases` is `[]`, while `docs/aide/failure_modes.generated.md` records the same mode as `validated` at the `synthetic-demonstrable` rung with three agreeing corpus cases — two committed conformance artifacts disagreeing about one mode, which is exactly what item 150 signs off. The cause is that `build_matrix` (`src/segfacet/traceability.py:488-500`, `:501`) still derives `cases`/`pipeline_detected` from the **geometric** manifest alone and `title` from `_vision_mode_titles()` alone, while item 146's round-2 fix moved only the mode *enumeration* to `failure_modes.SPECIFICATION`'s keys — mode 9 has no geometric case and no §6 numbered entry, so all three fields render as absence rather than as the intensity corpus's three pipeline-detected cases. Mode 9's and mode 10's `Evidence rung` cells render the literal `" -- "` for the same reason (`MODE_RUNGS.get(mode) or ModeRung(rung="", mechanism="")`), which reads as noise rather than as "no rung recorded". Re-pointing those fields at the specification is item 149's `build_matrix` work, not a review fix; found by the rank-3 per-item review of item 146 *(item 146, 2026-09-04, engine 1.37.0)* → item 156
- [x] defect — item 146's first `proposed` mode retired the "mode → rule is complete, always" contract that `src/segfacet/traceability.py` carried in both its `_NOTE` (restated by commit `72a67a5`) and its module docstring (restated in this review round), but the two documents the contract was authored in still assert it verbatim: `docs/aide/roadmap.md:887` ("**Yes, always** | a catalogued §6 failure mode nothing can detect. A defect, and this stage's primary finding") and `docs/aide/progress.md:948-949` ("**mode → rule** (complete, always — an uncovered §6 mode is a defect)"). Measured 2026-09-04 on `aide/queue-020`: `build_matrix()` reports `mode_to_rule.complete is False` with `holes == ('10',)`, and `derive_status(SPECIFICATION[10]) == 'proposed'` — so the shipped matrix is a standing counterexample to both sentences, and a reader who trusts either will read mode 10's hole as a defect rather than as the deliberate non-implementation it is. Neither file is editable from a review branch (`roadmap.md` is a root document authored through the create-roadmap entry point and PR-gated; `progress.md`'s passage is a dated 2026-08-11 record of what that roadmap said), so the reconciliation belongs to the next roadmap revision, which should say the direction is *scored* and that a `proposed` mode is an expected hole *(item 146, 2026-09-04, engine 1.37.0)* → item 156
- [x] defect — `tests/test_137_mode_less_rule_disposition.py`'s two AC18 invariance tests (`test_ac18_replacing_a_dispositioned_rules_declaration_leaves_run_rules_unchanged`, parametrised over `_DISPOSITIONED`, and `test_ac18_replacing_all_four_dispositioned_declarations_leaves_run_rules_unchanged`) are vacuous for all four rules: measured 2026-09-04, `run_rules(*_fixed_record())` returns **0 findings** on that record (its keys are `features_version`/`overlaps`/`per_label`/`relationships`/`stage3` — no `image_features`, no reference attached), so `after == before` is `[] == []` and would hold just as well if a replaced `mode_declaration` did reach `evaluate`. This is the same blind gate commit `1940ee0` diagnosed and fixed for item 146's own AC13, in the sibling module that AC18 lives in; commit `1b3067a` restored `intensity`/`intensity_reference_delta` to `_DISPOSITIONED` (correctly — the narrowing it undid was real), which widens the roll call feeding the vacuous gate rather than creating the vacuity. Two of the four are now driven live by `tests/test_146_ninth_mode_and_first_proposed.py::test_review_declaration_replacement_invariant_on_a_case_the_rule_fires_on`; `bounds` and `reference_delta` still have no record in that module they demonstrably fire on, and fixing that is item-137-scoped work, not a review edit *(item 146, 2026-09-04, engine 1.37.0)* → commit 1fa31f8: AC18 now evaluates implausible_metal with the bundled reference, where all four rules fire; precondition asserted, verified failing when a declaration leaks into evaluation
- [ ] defect — `docs/aide/vision.md` §6's evidence-rungs paragraph calls mode 7's `L1 → T12 → L2 → L5` example a "two-descent" input, the same inaccuracy `insights.md` (item 145, 2026-09-03) recorded in `traceability.py::MODE_RUNGS[7]` and `failure_modes.py` mode 7. Measured 2026-09-04 from `segfacet.labels`: the example's canonical ranks are (20, 18, 21, 24) and its label values (20, 19, 21, 24) — exactly **one** descent under either reading. Item 147 settles the correct sentence in the specification, but `vision.md` is a root document and is not edited from inside an item (queue-020 scope fence), so §6 keeps the wrong parenthetical until a re-issue through `/aide-create-vision` corrects it *(item 147, 2026-09-04, engine 1.37.0)*
- [x] defect — `tests/test_136_rule_mode_declarations.py::test_ac8_surplus_declared_mode_is_reported_naming_both` constructs `RuleModeDeclaration(modes=new_modes, evidence=("corpus",))` and asserts `catalogue.rule_declaration_conflicts()` reports the surplus mode, relying on exactly the declared→corpus direction item 147 step 8 deletes (`if "corpus" in decl.evidence:`). Item 147's spec authorises only three named `test_136` reconciliations (`test_ac4_corroborated_rule_declares_corpus_modes`, `test_ac10_corpus_tagged_declared_modes_subset_of_corpus_map`, `test_adv_corpus_tag_plus_other_tag_still_binds_ac8_direction`); this fourth test sits right beside the second of those (same corpus-map-derived surplus-mode setup) but is not in the list, so once item 147 lands its `assert conflicts, "expected at least one conflict"` goes red on a direction the item deliberately retired, not a regression. It needs the same treatment as its authorised neighbours — retire or re-point at `failure_modes.specification_conflicts()`'s AC13 shape — found while writing item 147's tests, 2026-09-04 *(item 147, 2026-09-04, engine 1.37.0)* → item 159
- [x] defect — `src/segfacet/eval/severity_ladder.py` carries the same false mode-7 claim item 147 corrected in the specification, in two places: the module docstring (`rank(v) == v - 1` for every value 1-24, so no in-block relabel can produce a rank descent) and the mode-7 ladder's own `rationale` string, which reaches no committed artifact but is the recorded justification for declaring that axis `severity_kind="degenerate"` with 2 rungs. Measured 2026-09-04 from `segfacet.labels`: `CANONICAL_ORDER` inserts `T13` at index 19, so `L1`-`L5` have canonical rank equal to their integer value and only labels up to `T12` have rank == value - 1 — the premise the degeneracy argument rests on is false for exactly the lumbar block the corpus fixtures use. Whether the *conclusion* (out_of_order_label_count structurally capped at 1.0) survives the corrected premise is a Stage-21 question, not item 147's: the file is outside its authorised paths, and item 147's AC10 tree-wide scan for the literal `rank(v) == v - 1` fails on it today *(item 147, 2026-09-04, engine 1.37.0)* → item 154
- [x] defect — three of `tests/test_147_specification_is_the_record.py`'s checks cannot pass as written against item 147's own spec. `test_ac3_...` requires the set of `src/segfacet/` files mentioning `MODE_ANCHOR_PATHS` to be exactly four, but `heuristics/reference_delta.py:128` names it in a **comment** explaining why mode 1 is declared alongside mode 2; `test_ac4_vision_parse_has_one_home` greps for the literal `vision.md` and hits `src/segfacet/__init__.py:4` (a docstring pointer) and `heuristics/intensity.py:135` (a comment) — neither *reads* the document, which is what AC3/AC4 are about, and all three files are outside item 147's authorised paths. `test_ac9_every_mechanism_names_a_token_that_resolves_live[10]` is unsatisfiable by construction: it builds its candidate-token set from `MODE_ANCHOR_PATHS[10]` (absent by AC3), `corpus_cases` (empty) and `intended_rules` (empty), while the spec's own Implementation step 2 says mode 10's mechanism should name its **candidate feature** — the one source the test omits. Either the greps narrow to reads/references-in-code, or the three files are authorised for a comment-only edit; either way it is a spec/test call, not a builder one *(item 147, 2026-09-04, engine 1.37.0)* → item 159
- [x] defect — `src/segfacet/heuristics/intensity.py:151` and `src/segfacet/heuristics/intensity_reference_delta.py:145` each carry a `mode_declaration` `evidence` sentence asserting that binding the reserved `"corpus"` tag "would report a false conflict" against `catalogue._scan_synth_rule_mode_map()`'s geometric-only AST scan. Item 147 retired both the tag and the `if "corpus" in decl.evidence:` branch that gave it force, so that consequence no longer exists — measured 2026-09-04 by `tests/test_147_specification_is_the_record.py::test_adv_ac20_corpus_tag_no_longer_binds_special_behaviour`, which asserts `catalogue.rule_declaration_conflicts()` returns an identical tuple with and without the tag. `intensity.py`'s copy at least appends "(item 146 A6; item 147 retires the tag)"; `intensity_reference_delta.py`'s carries no caveat at all. Both strings render verbatim into the Evidence column of the committed `docs/aide/traceability_matrix.generated.{md,json}`, so the maintainer signing that artifact off at item 150 reads a retired mechanism described as live. Both files are outside item 147's authorised paths (its six `heuristics/` entries are border, sequence, fragmentation, overlap, mislabel and coverage) and correcting them regenerates that artifact, so it is item 148's declaration-seam work rather than a review fix *(item 147, 2026-09-04, engine 1.37.0)* → review 148: intensity evidence strings
- [x] gap — after item 147 nothing reports a rule declaring a **known** mode that the specification's own `intended_rules` do not mirror. `failure_modes._intended_rule_conflicts` checks specification → declaration (AC13) and `catalogue.rule_declaration_conflicts` checks corpus → declaration plus "declared mode outside `SPECIFICATION`'s key set", but a declaration naming a listed mode with no matching `IntendedRule` edge and no corpus case passes every check: measured 2026-09-04 on `aide/queue-020`, replacing `reference_delta`'s declared modes with `(1, 2, 5)` yields `()` from both `catalogue.rule_declaration_conflicts()` and `failure_modes.specification_conflicts()` while `build_matrix()` silently lists mode 5's rules as `('coverage', 'reference_delta')`, which regenerates into `docs/aide/traceability_matrix.generated.*` as a claim no check made. The retired `"corpus"`-gated branch bound this direction for the six corpus-corroborated rules only; that force survives for those six through `tests/test_136_rule_mode_declarations.py`'s `test_ac4_corroborated_rule_declares_corpus_modes` + `test_ac4_corroborated_modes_match_measured_corpus_map` equality pair (not through item 145's AC8, which iterates specification edges rather than declarations, as item 147's assumption A8 states), leaving `bounds`, `reference_delta`, `intensity` and `intensity_reference_delta` unbound in that direction — the seam item 148 rewrites *(item 147, 2026-09-04, engine 1.37.0)* → item 156
- [ ] gap — a `signal` path classification (item 148) is an authored claim no shipped check can refute; refuting one needs a per-path perturbation harness (a threshold-crossing mutation per signal path, asserting the rule's finding list moves), and above that per-DETECTOR attribution: mislabel's offset detector serves §6 mode 1 and its monotonicity detector mode 4, fragmentation's two branches modes 2 and 3, so those signal paths stay attributed one mode too wide. Both need rule-side first-class detector ids — IntendedRule.detector is authored prose, and joining a mechanical check on it is the retired "corpus"-tag defect *(item 148, 2026-09-04, engine 1.37.0)*
- [ ] defect — item 148's Testing Strategy "existing tests to reconcile" list for `tests/test_137_mode_less_rule_disposition.py` omits two pins that break as a direct, mechanical consequence of this item's own AC9/AC15: `_CANONICAL_TAG_ORDER` (a 4-tuple) feeds `test_ac11_mode_evidence_is_canonical_subsequence_or_unmapped`'s `.index(tag)` lookup over every shipped entry, so the moment any entry legitimately carries `"rule_bookkeeping"`/`"rule_not_read"` (e.g. the top-level `per_label` container) that test raises `ValueError`, not a failed assertion; `test_ac15_catalogue_artifacts_regenerate_byte_identically` separately pins `payload["schema_version"] == "1.1"`, which item 148's own SCHEMA_VERSION bump to `"1.2"` falsifies. Both are the same defect class as item 147's `test_ac8_surplus_declared_mode_is_reported_naming_both` gap recorded above: a reconciliation list that is a strict subset of what the shape change actually touches. Fixed in-place while writing item 148's tests (test-writer's edit scope covers this file for reconciliation; the two additional fixes are the same "mode_evidence tag-set pin" / schema-version-pin categories the task's own instructions named), so no builder action is needed here — recorded so the next Testing-Strategy author checks a changed file for every pinned literal, not only the bullets a human enumerated by hand *(item 148, 2026-09-04, engine 1.37.0)*
- [x] defect — two committed tests fail against item 148's own spec, both test-side. (1) `tests/test_148_per_path_mode_attribution.py`'s `_status_report_module()` helper omits the `sys.modules[spec.name] = module` line its model carries (`tests/test_103_feature_catalogue.py:861`), so importing `scripts/aide_status_report.py` — which has `from __future__ import annotations`, making every dataclass field annotation a string the decorator resolves through `sys.modules` — raises `AttributeError: 'NoneType' object has no attribute '__dict__'` from `dataclasses._is_type`, and `test_ac15_schema_version_and_status_report_loader` fails before reaching its own assertion; with the line added, `load_feature_catalog` returns a non-empty `FeatureGroupSpec` tuple as AC15 requires. (2) `tests/test_103_feature_catalogue.py::test_ac13_rule_mode_map_effect_on_failure_modes[overlap-modes4]` requires a non-anchor, `signal`-classified entry consumed only by each rule, but `overlap`'s only `signal` path under item 148's own authored table is `overlaps[].overlap_voxels`, which is also `MODE_ANCHOR_PATHS[8]`'s sole member — the match set is empty by construction for `overlap` alone, and widening `overlap`'s classification to satisfy it would ship the false claim the item exists to remove, so the fix is a per-rule exemption in the test. Builder is not authorised to edit either module *(item 148, 2026-09-04, engine 1.37.0)* → item 159
- [x] defect — item 148 classifies `stage3.per_label_offsets[].dx_mm`, `.dy_mm` and `.dz_mm` as `signal` for `mislabel`, but `MislabelRule.evaluate` reads all three only through `MislabelRule._dominant_direction` (`src/segfacet/heuristics/mislabel.py:313`), whose result reaches nothing but the finding's `", predominantly <axis>"` clause at `mislabel.py:378` — the firing decision is `offset >= max_offset` on `offset_mm` alone. Under item 148's own vocabulary that is "message interpolation", which the same declaration classifies `bookkeeping` for `.level_name` and which `reference_delta` classifies `bookkeeping` for `…physical_volume_mm3.value` and `.percentile_rank` on identical grounds, so the three paths keep `failure_modes == (1, 4)` in `docs/aide/feature_catalogue.generated.*` where the item's own rationale would leave them `()` with `("rule_bookkeeping",)`. Measured 2026-09-04 on `review/148-findings`. Reclassifying is an authored-claim change that moves a committed artifact and contradicts the item-148 spec's own step-2 table, so it is a decision for item 149/150 rather than a review fix *(item 148, 2026-09-04, engine 1.37.0)* → decided at item 150 (2026-09-14): offset_mm, dx_mm, dy_mm, dz_mm reclassified bookkeeping in heuristics/mislabel.py -- Detector A serves no failure mode
- [x] gap — the traceability matrix's rule-attribution column silently covers only the geometric corpus, and nothing reports the resulting contradiction. `segfacet.catalogue._scan_synth_rule_mode_map` (`src/segfacet/catalogue.py:690-722`) matches an `ast.Call` whose func name is literally `Expectation` and which carries an int-literal `failure_mode=` plus an `expected_rule_ids=` set; the intensity corpus's four cases are `_RecipeEntry(...)` literals (`src/segfacet/synth/intensity.py:326-366`) using `expected_firing=` and the named constant `_IMPLAUSIBLE_TISSUE_MODE_ID`, so the scan matches none of them. Measured 2026-09-04 on `review/148-followup`: `scan_synth_rule_mode_map()` returns exactly the six geometric rules (border, coverage, fragmentation, mislabel, overlap, sequence) with no `intensity` key, so `docs/aide/traceability_matrix.generated.md`'s mode-9 row renders `intensity (analytic), intensity_reference_delta (analytic)` — "analytic" meaning "only the rule's own declaration claims it" (`src/segfacet/traceability.py:52-56`) — directly beside a `synthetic-demonstrable` evidence rung stating that three committed intensity-corpus cases drive `intensity` end-to-end, which re-measurement confirms (`segfacet.synth.regression.intensity_pipeline_findings`: implausible_metal, implausible_soft_tissue and degenerate_uniform each fire `['intensity']`, clean_hu fires none). Until commit `cc01ed5` the two intensity rules' `evidence` strings explained the geometric-only scan in prose; that explanation was correctly removed along with the false "would report a false conflict" consequence it was attached to (the ticked 2026-09-04 entry above naming `src/segfacet/heuristics/intensity.py:151`), leaving the seam undocumented at the artifact item 150 signs off. The fix is to make the second committed corpus scannable, or to qualify the attribution term where it is rendered — not to re-add prose to a declaration *(item 148, 2026-09-04, engine 1.37.0)* → item 149 Decision D2 (docs/aide/items/149-the-traceability-matrix-becomes-the-conformance-report.md) - attribution re-derived from SPECIFICATION corpus_cases
  - **2026-09-15** → no longer relevant as captured. Item 149's Decision D2 derives the matrix's attribution column from `failure_modes.SPECIFICATION[*].corpus_cases`, which span both corpora, so the contradiction this entry names is gone: `docs/aide/traceability_matrix.generated.md` renders the intensity mode (mode 9 when captured, mode 16 after the item-150 sign-off) as `intensity (corpus), intensity_reference_delta (analytic)`, verified at `7e4bb5c` and replayed clean by item 151. The rule and the fix direction still stand in one narrower place: `catalogue.scan_synth_rule_mode_map` is still geometric-only, and it now feeds `traceability.build_matrix` only for `corpus_designated_unregistered_rule_ids` (`src/segfacet/traceability.py:450`, `:573-577`) plus the feature catalogue (`src/segfacet/catalogue.py:892`, `:1162`), which was not re-checked here. Roadmap v3.1's Stage 31 D4 bullet still describes the pre-D2 rendering ("mode 16's attribution renders analytic"), so read it through this line when that queue is planned.
- [ ] knowledge — the per-label spline offset (`stage3.per_label_offsets[].offset_mm`) is an anatomy-classification signal (spondylolisthesis, scoliosis grading), not a failure signal: after the item-150 sign-off `mislabel`'s Detector A serves no failure mode, its read paths are classified `bookkeeping`, and the feature belongs in a clinical-descriptor group when Stage 27 re-taxonomises the feature schema *(item 150, 2026-09-14, engine 1.37.0)*
- [x] gap — the Stage-18/29 eval harness (`segfacet.eval.per_mode`, `severity_ladder`, `per_mode_cohort`, their JSON schemas and the `test_099`-`test_102`/`test_109`/`test_125`/`test_135` pins) is still keyed by the pre-sign-off mode ids 1-8, frozen as `eval.per_mode.LEGACY_STAGE18_MODE_NAMES`; re-keying it to the signed-off catalogue needs re-measured ladder constants (`KNOWN_CROSS_MODE_COUPLINGS`, `RECORDED_MARGINS`), and the `displace` ladder then corresponds to no mode at all. One follow-up item, after the vision §6 re-issue *(item 150, 2026-09-14, engine 1.37.0)* → item 153
- [x] gap — `docs/aide/vision.md` §6's numbered eight-item list no longer matches the catalogue (`segfacet.failure_modes.SPECIFICATION` now assigns the ids, in a one-tier hierarchy, with one seed title retired and one re-homed as a condition); §6 must be re-issued through `/aide-create-vision` as principles plus a pointer to the specification, never a numbered list, and until then `failure_modes.VISION_SEED_DISPOSITION` carries the seed-to-record provenance *(item 150, 2026-09-14, engine 1.37.0)* → item 152
- [ ] gap — three detectors the sign-off asked for that no shipped rule provides: a spacing-gap detector over `stage3.spacing_consistency.spacings_mm[]` for mode 4 (its fixture `remove_level_relabel` fires nothing today), an unprompted-transitional-label detector over `relationships.present_levels[]` plus a configuration flag for mode 6 (T13/L6 in a scan not configured for them), and a centroid-based collapsed/duplicated detector for mode 8 that needs per-component centroids the feature layer does not extract. Each is a rule item, not a specification edit *(item 150, 2026-09-14, engine 1.37.0)*
- [ ] gap — mode 2's lumbosacral transitional-anatomy sub-type (sacralised L5, lumbarised S1) wants an intervertebral-disc label channel -- disc labels lying inside the sacrum label are the hypothesised signal -- which `segfacet.labels.DEFAULT_LABEL_MAP` does not carry; needs a decision on a second label channel before any rule can read it *(item 150, 2026-09-14, engine 1.37.0)*
- [ ] knowledge — `synth.component_shape.FusePerturbation` fuses unbridged (the absorbed neighbour's voxels keep their gap), so `fuse_adjacent` is detected only by co-detections (fragmentation on two bodies under one label, coverage on the absorbed level); a bridged fuse operator that fills the inter-body gap is the honest fixture for mode 2's own bounds / reference_delta volume proxy, and would let mode 2 validate *(item 150, 2026-09-14, engine 1.37.0)*
- [ ] gap — per-detector attribution is now a live need, not a nicety: `mislabel` has one detector serving mode 6 and one serving no mode, and the only way item 148's per-path schema can say so is to classify Detector A's `offset_mm`/`dx_mm`/`dy_mm`/`dz_mm` as `bookkeeping`, so the catalogue renders a detector's firing signal under `rule_bookkeeping`. The rule-side detector ids insight 50 asks for would replace that workaround *(item 150, 2026-09-14, engine 1.37.0)*
- [ ] knowledge — the corpus case ids keep their historical `modeN_` prefixes as stable identifiers (43 test modules name them), and after the item-150 re-keying the prefixes lie: `mode2_fragment` is mode 3, `mode5_remove_level` is mode 6, `mode6_crop_at_border` carries no mode. A reader or test keying off the prefix is wrong; the manifest's `failure_mode` field is the authority. Renaming the ids is a corpus-value change for a later item *(item 150, 2026-09-14, engine 1.37.0)*
- [x] gap — `failure_mode == 0` in a corpus manifest now means two things: a clean control, or a condition-only case (`condition` non-empty). `synth.regression.verify_case`, `traceability._build_conformance` and `failure_modes._corpus_case_conflicts` distinguish them; any other consumer filtering on `failure_mode != 0` silently drops the crop case (test_040's rebuild sweep did until reconciled). A dedicated sentinel or a `kind` field would remove the trap *(item 150, 2026-09-14, engine 1.37.0)* → item 155
- [ ] knowledge — a document-wide scan of `docs/aide/failure_modes.generated.md` for a feature path is unsafe: mode 2's mechanism legitimately names mode 3's anchor path and renders first, so a first-occurrence search lands in the wrong entry. Any test or doc tool scanning the rendering must scope the search to the `## Mode N` section (test_144 AC20 was caught by exactly this) *(item 150, 2026-09-14, engine 1.37.0)*
- [ ] knowledge — a human-gate row whose prose quotes a source path (the Stage-30 sign-off gate names `src/segfacet/failure_modes.py`) makes any "no `aide check` warning names a path this item writes" acceptance criterion unstable while the gate is awaiting; that AC shape must exclude gate warnings from the start, the same class as pinning a warning count *(item 150, 2026-09-14, engine 1.37.0)*
- [ ] knowledge — `feature_docs.MODE_ANCHOR_PATHS[1]` stays `stage3.per_label_offsets[].offset_mm` (the Stage-18 mode-1 metric's recorded read path) while the item-150 sign-off reclassified `mislabel`'s offset paths as `bookkeeping` serving no mode, so mode 1's anchor and mode 1's mechanism disagree about which rule reads it; the eval re-key item should re-anchor mode 1 on a volume/extent path *(item 150, 2026-09-14, engine 1.37.0)*
- [ ] knowledge — the item-150 insight lines dated 2026-09-14 name mode ids of that day's catalogue, which the 2026-09-15 revision of the same review re-assigned: old 1 → 1, old 2 (fused or split) → 2 fused and 3 split, old 3 (islands) → 4, old 4 (not segmented) → 6, old 5 (mislabelling) → 8, old 6 (implausible sequence) → 9 out-of-order, 10 missing interior level and 11 unprompted variant, old 7 (shifted) → 12, old 8 (collapsed or duplicated) → 13 collapsed and 14 duplicated, old 9 (overlap) → 15, old 10 (tissue) → 16; new are 5 (holes) and 7 (hallucinated vertebra). `mode2_fragment` moved from old 3 to mode 1. Read those lines through this map *(item 150, 2026-09-15, engine 1.37.0)*
- [ ] gap — detectors and fixtures the 2026-09-15 split asked for that nothing provides: an enclosed-cavity count/volume or Euler-characteristic feature for mode 5 (holes), an above-expected-count check for mode 7 (hallucinated vertebra; `coverage`'s count check tests only a shortfall), and a split operator that reassigns part of one label to its neighbour so mode 3 (split vertebra segment) has a corpus case. Each is a feature, rule or corpus item, not a specification edit *(item 150, 2026-09-15, engine 1.37.0)*
- [ ] knowledge — mode 10 (missing interior level) sits under semantic mislabelling (mode 8) but, unlike its siblings, need not contain a mislabelling: a missed vertebra with correct labels leaves the same gap. The one-tier hierarchy has no label-sequence parent to hold it, so its discriminator records the exception; a top-level label-sequence mode is the alternative if more sequence modes without a mislabelling appear *(item 150, 2026-09-15, engine 1.37.0)*
- [ ] knowledge — superseding the entry above: the maintainer resolved it the same day by narrowing mode 10 to a skipped level label on segmented vertebrae (renamed *Skipped level label*, severity `fail`, `proposed`), so it always contains a mislabelling; a label gap left by a missed vertebra is mode 6's, which now carries `coverage`'s interior-gap detector and `mode5_remove_level`. What separates the two from the label map is the centroid spacing across the gap, which no rule reads; a skip-relabel fixture and a spacing-aware detector are owed to mode 10 *(item 150, 2026-09-15, engine 1.37.0)*
  - **2026-09-15** → one stale copy of the pre-move attribution remains. `docs/aide/items/150-maintainer-sign-off-of-the-specification.md`'s entry-by-entry Mode 2 line says `fuse_adjacent` fires by "co-detections by modes 1's and 10's detectors". Since the move this entry records, `coverage`'s interior-gap detector serves mode 6, and both `SPECIFICATION[2].mechanism` and the `fuse_adjacent` corpus-case reason say "mode 1's and mode 6's detectors" (verified 2026-09-15, item 151). The merged spec is a record and is not edited, so read that line as mode 6.
- [x] knowledge — the open item-148 gap entry (2026-09-04) saying the traceability matrix renders mode 9's `intensity` edge `analytic` beside a demonstrated rung no longer describes the committed artifact: item 149's Decision D2 re-derived attribution from `SPECIFICATION[*].corpus_cases`, and `docs/aide/traceability_matrix.generated.md` at `7e4bb5c` renders mode 16's row `intensity (corpus), intensity_reference_delta (analytic)`; `catalogue.scan_synth_rule_mode_map` is still geometric-only but now feeds only `corpus_designated_unregistered_rule_ids`, so roadmap v3.1's Stage 31 D4 bullet naming this seam wants re-reading at that queue's planning *(item 151, 2026-09-15, engine 1.37.0)* → folded into the 2026-09-15 trail line beneath the item-148 (2026-09-04) entry on the geometric-only rule-attribution column, where a status change belongs
- [x] knowledge — `docs/aide/items/150-maintainer-sign-off-of-the-specification.md`'s entry-by-entry Mode 2 line says `fuse_adjacent`'s firing is "co-detections by modes 1's and 10's detectors", but after the same review's 2026-09-15 revision `coverage`'s interior-gap detector serves mode 6, and both `SPECIFICATION[2].mechanism` and the `fuse_adjacent` reason already say mode 6; the transcript line is the only stale copy, and a merged spec is a record, so it is read through this note rather than edited *(item 151, 2026-09-15, engine 1.37.0)* → folded into the 2026-09-15 trail line beneath the item-150 (2026-09-15) entry narrowing mode 10 to *Skipped level label*, which records the detector move
- [x] framework — `.aide/AGENT-CONTEXT.md` line 79 lists the CLI verbs as `insights list/tick/archive`, omitting `insights resolve` (engine 1.43.0), so the always-on floor names three of the four insight verbs and a session that hits an `insights.md` merge conflict is not told the verb that resolves it *(2026-09-16, engine 1.52.1)* → aide-loop#231
- [x] framework — nothing in the standard loop runs `aide check` mechanically: CI runs only `aide scope`, `aide merge` does not call `run_checks` before or after its test gate, and the validator brief names `aide check` only for human gates — so a progress.md that stops parsing or rolling up (the Stage 20 ✅-over-⏸️ error of 2026-09-03, seen only at the next engine update) is caught by nothing unless a consumer pins it in its own suite, which five per-item AC tests did here until they were retired for pinning the warning set (§6). `aide merge`'s post-merge gate, or the validator's step 3, wants an in-process `run_checks` with errors blocking *(2026-09-16, engine 1.52.1)* → aide-loop#232
- [ ] knowledge — removing `vision.md` §6's numbered seed list (Stage 31 D1, draft PR #77) reds exactly nine tests, all readers of `failure_modes.vision_seed_titles()` or of §6's numbered form directly: `test_137::test_ac17_vision_section_six_still_has_exactly_eight_modes`, `test_138::test_ac9_vision_section_six_titles_are_dispositioned_provenance`, `test_144::test_ac16_vision_section_six_seed_titles_all_have_a_resolving_disposition`, `test_144::test_ac16_vision_seed_conflicts_reports_an_unresolvable_disposition`, `test_145::test_ac3_every_vision_seed_title_has_a_resolving_disposition`, `test_147::test_ac4_vision_parse_has_one_home`, `test_147::test_ac5_every_vision_seed_title_disposes_and_resolves`, `test_147::test_adv_ac5_unresolvable_disposition_is_reported`, `test_151::test_ac27_vision_seed_conflicts_is_empty` (measured 2026-09-16 on `docs/vision-v4-section-6`, 9 failed / 667 passed over the eight §6-reading modules). `vision_seed_titles()` returns `{}` rather than raising, and `vision_seed_conflicts()` then reports every disposition title as absent. This is D2's whole surface: those three symbols become provenance-only or retire, and `test_144`'s private `_vision_mode_titles()` parse goes with them *(2026-09-16, engine 1.52.1)*
- [x] defect — `tests/test_150_maintainer_sign_off.py::test_ac14_every_item_150_insight_is_well_formed_and_honestly_dated` is red on `aide/queue-021` (confirmed identical on `origin/aide/queue-021` and on item 152's branch, which touches neither file): the item-150-provenance line at `docs/aide/insights.md` (the "Stage-18/29 eval harness ... still keyed by the pre-sign-off mode ids" entry, dated 2026-09-14) now ends with a routing suffix `→ item 153`, appended by a later triage pass, which `test_150`'s `_ITEM_150_GRAMMAR_RE` does not tolerate after the closing `*(...)*` provenance parenthetical — the regex anchors the match at the parenthetical's end. Full-suite run on item 152's branch (`aide/152-retire-the-vision-6-seed`, 2026-09-16): 1 failed, 8359 passed, 63 skipped, all environment-gated. Either the grammar regex needs to tolerate a trailing `→ item NNN` pointer (the shape every routed entry above it already carries), or routing must stop appending text after the provenance parenthetical for item-150-tagged lines specifically. Neither `docs/aide/insights.md` nor `tests/test_150_maintainer_sign_off.py` is in item 152's Authorised paths, so this is queue-021's defect to fix, not item 152's *(item 152, 2026-09-16, engine 1.52.1)* → ecdc81d (queue-021 fix commit)
- [ ] framework — `aide sync --item NNN` runs `git pull --rebase origin <claim>` with no `_has_unpushed_merge` guard (the bookkeeping-commit pull has one): on a claim branch holding a local, unpushed merge commit — item 152's A4 merge of the vision v4 commit `7d800a2` — the rebase drops the merge and replays both parents, which stopped mid-rebase on a `docs/aide/vision.md` conflict and left the shared checkout mid-rebase for the concurrently running validator and reviewer (reflog, 2026-09-16; an earlier run had already silently linearised the same merge). Recovered by `git rebase --abort` and pushing the claim branch. `sync --item` wants the same unpushed-merge refusal the bookkeeping pull has *(item 152, 2026-09-16, engine 1.52.1)*
- [ ] knowledge — roadmap Stage 31 D3 says the `displace` severity ladder "measures the spline-offset signal, which after item 150 serves no mode"; it does not: `severity_ladder.py`'s `displace` ladder designates `unanchored_foreground_fraction`, which `SPECIFICATION[1].candidate_features` cites and whose corpus case `mode1_displace` mode 1 lists, so it re-homes to mode 1; the spline-offset path is only `feature_docs.MODE_ANCHOR_PATHS[1]`, that metric's catalogue anchor, re-anchored by item 154. For the next roadmap revision *(item 153, 2026-09-16, engine 1.52.1)*
