<!--
  AIDE insight inbox. Any role appends ONE line here, at any time, when it
  learns something true but out of scope for its task, and returns to that
  task. The engine creates docs/aide/insights.md from this file, byte for
  byte, the first time `aide check`, `claim`, `queue start` or `insights
  list` finds it missing; nobody copies it by hand.
  The rules — what is captured, what is immutable, what each verb does — are
  conventions.md §1 → insights.md and `python .aide/scripts/aide.py insights
  -h`. This comment is the shapes; the aide-template line below it names
  the template version this inbox was created from.

  Entry:
    - [ ] <type> — <one line> *(<provenance>, YYYY-MM-DD, engine X.Y.Z)*
  The date is required; the provenance before it and the engine version
  after it are free-form and optional. Conventional provenance spellings:
    *(item 099, 2026-07-26)*           one item
    *(items 099-101, 2026-07-27)*      a finding that spans several
    *(queue-014, 2026-07-26)*          planning or spec-authoring, before any item
    *(2026-07-26)*                     no item or queue to name
    *(item 099, 2026-07-26, engine 1.22.0)*   with the engine it was observed under
  Types:
    knowledge  — true fact worth documenting
    defect     — something is wrong and needs a fix item
    gap        — something is missing and needs planning
    automation — a recurring manual/agent action that code could replace
    framework  — belongs to AIDE itself, not this project

  A triaged entry, ticked in place with a pointer:
    - [x] <type> — <one line> *(item NNN, YYYY-MM-DD, engine X.Y.Z)* → <where it landed>
  A status trail, appended under an entry after triage, newest last:
    - [x] framework — <the original claim, never touched> *(2026-08-20)*
      - **2026-08-20** → aide-loop issue #50
      - **2026-10-11** → resolved in engine 1.16.0
-->
<!-- aide-template: insights 1 -->
# Insight Inbox

_Entries below, newest last._

- [ ] gap — a `signal` path classification (item 148) is an authored claim no shipped check can refute; refuting one needs a per-path perturbation harness (a threshold-crossing mutation per signal path, asserting the rule's finding list moves), and above that per-DETECTOR attribution: mislabel's offset detector serves §6 mode 1 and its monotonicity detector mode 4, fragmentation's two branches modes 2 and 3, so those signal paths stay attributed one mode too wide. Both need rule-side first-class detector ids — IntendedRule.detector is authored prose, and joining a mechanical check on it is the retired "corpus"-tag defect *(item 148, 2026-09-04, engine 1.37.0)*
  - **2026-09-17** → left open: the per-detector half is re-homed with the item-150 (2026-09-14) per-detector entry to roadmap Stage 32's mode 9 input; the per-path perturbation harness that would refute a signal classification has no stage or mode home
  - **2026-09-22** → left open: the per-detector half shipped as item 164's first-class detector ids; the per-path perturbation harness is still unowned and is not in Stage 33 (scoped 2026-09-22)
- [ ] knowledge — a document-wide scan of `docs/aide/failure_modes.generated.md` for a feature path is unsafe: mode 2's mechanism legitimately names mode 3's anchor path and renders first, so a first-occurrence search lands in the wrong entry. Any test or doc tool scanning the rendering must scope the search to the `## Mode N` section (test_144 AC20 was caught by exactly this) *(item 150, 2026-09-14, engine 1.37.0)*
- [ ] knowledge — the item-150 insight lines dated 2026-09-14 name mode ids of that day's catalogue, which the 2026-09-15 revision of the same review re-assigned: old 1 → 1, old 2 (fused or split) → 2 fused and 3 split, old 3 (islands) → 4, old 4 (not segmented) → 6, old 5 (mislabelling) → 8, old 6 (implausible sequence) → 9 out-of-order, 10 missing interior level and 11 unprompted variant, old 7 (shifted) → 12, old 8 (collapsed or duplicated) → 13 collapsed and 14 duplicated, old 9 (overlap) → 15, old 10 (tissue) → 16; new are 5 (holes) and 7 (hallucinated vertebra). `mode2_fragment` moved from old 3 to mode 1. Read those lines through this map *(item 150, 2026-09-15, engine 1.37.0)*
- [ ] knowledge — the corrected `sequence_break` rank premise (item 154) measures more than the old "capped at 1" claim: applying `sequence_break(target_label=24, new_label=28)`, then `(23, 27)`, then `(22, 29)` cumulatively to the geometric corpus's default base leaves present labels `[20, 21, 27, 28, 29]` with canonical ranks `[20, 21, 32, 19, 27]` — 2 descents (measured live via `extract_feature_record`/`compute_per_mode_metrics`, `out_of_order_label_count == 2.0`, matching an independent hand count from `labels.CANONICAL_ORDER`/`DEFAULT_LABEL_MAP`). The `sequence_break` ladder still ships 2 rungs (its single default step), not re-graded; a graded `sequence_break` ladder using multi-step relabels past value 27 is a Stage 32 option *(item 154, 2026-09-16, engine 1.52.1)*
- [ ] gap — the eval harness groups corpus cases by `CaseOutcome.failure_mode` (`segfacet.eval.metrics._compute_per_mode`), so the FOV-truncation condition case lands in the mode-0 per-mode bucket beside the clean control and is reported under the clean-control name when `failure_modes=FAILURE_MODE_NAMES` is passed; `tests/test_116_ras_native_corpus.py::test_ac8_mode6_crop_at_border_sensitivity_is_restored_to_one` pins that bucket (`m.failure_mode == 0`). Item 155 adds the manifest `kind` field and moves manifest-dict consumers onto it, but the eval harness reads typed attribute-form records and is outside that sweep; a condition-keyed bucket is an eval-harness schema change *(item 155, 2026-09-16, engine 1.52.1)*
  - **2026-09-17** → left open: a condition-keyed per-mode bucket is an eval-harness schema change that no stage deliverable or mode input names
  - **2026-09-22** → routed to roadmap.md Stage 33 D3 (condition-keyed eval bucket, needed once there are two conditions)
- [x] gap — item 155's `_zero_comparisons` AST scan (`tests/test_155_corpus_case_kind.py`) only flags `Eq`/`NotEq`/`Is`/`IsNot` single-op comparisons and `not` truthiness against a failure_mode access; `in (0,)`/`in {0}` membership tests, chained comparisons, and `bool(...)`-wrapped truthiness on the same access would pass the scan undetected, reopening the class of bug item 150's `failure_mode == 0` double-meaning caused *(item 155, 2026-09-16, engine 1.52.1)* → item 184
  - **2026-09-17** → left open: a hardening of tests/test_155_corpus_case_kind.py's AST scan; no stage owns test-hygiene work now that Stage 29 is closed, so it waits for a maintenance queue
  - **2026-09-22** → left open: no interaction with Stage 33's surfaces (scoped 2026-09-22); waits for a maintenance queue
- [ ] knowledge — the item-147 gap entry's `(1, 2, 5)` control (2026-09-04, above, ticked → item 156) is no longer fully silent the way it measured that day: item 150's re-keying made mode 5 a `proposed` mode, so `failure_modes.specification_conflicts()` now fires its proposed-drift check on mode 5 before item 156's declaration → specification direction gets a chance to name `reference_delta` — measured 2026-09-16, `specification_conflicts()` on the widened declaration returns 4 messages (proposed-drift for mode 5, plus spec → declaration for modes 3, 4, 8), none naming `reference_delta` together with mode 5. The seam itself is unaffected (item 156's new `catalogue.rule_declaration_conflicts()` direction still reports it, per AC3), but a future reader replaying this control against `specification_conflicts()` alone, expecting `()`, will see 4 messages and none of them will look related *(item 156, 2026-09-16, engine 1.52.1)*
- [x] gap — committed_artifact_guard's resolver has no branch for the os.path.dirname(os.path.abspath(__file__)) root idiom (live in tests/test_019_vertebra_orientation_curvature.py, test_121_tangent_orientation.py, test_122_signed_curvature.py, test_131_tangent_direction_normalisation.py, currently only for sys.path setup, hiding no comparison today); it's the same defect class as item 158's two fixes and is absent from the docstring's "Still skipped in silence" list. *(item 158, 2026-09-17, engine 1.52.1)* → item 185
  - **2026-09-17** → left open: the same defect class as item 158's fixes, hiding no comparison today (item 158, 2026-09-17); no stage owns it, so it waits for a maintenance queue
  - **2026-09-22** → left open: no interaction with Stage 33's surfaces (scoped 2026-09-22); waits for a maintenance queue
- [ ] knowledge — src/segfacet/__init__.py's PEP-562 __getattr__ (item 159) makes segfacet.features unavailable as a bare attribute until a lazy name is first touched or it's imported directly; every current caller already imports the submodule explicitly, so nothing breaks today, but a future bare `segfacet.features.X` access would raise AttributeError where it silently worked before *(item 159, 2026-09-17, engine 1.52.1)*
- [x] automation — the suite's wall-clock nearly doubled with no CI change: `pytest -n 4 --durations=40` on `main` at 2fe6ef6 measured 8989 tests in 8m35s (Linux, 2026-09-18) against the 6690 in 4m51s that `.github/workflows/ci.yml`'s Test step records for 2026-09-01. Two repeated-work clusters account for roughly 5 of the ~34 CPU-minutes: (a) about twelve tests each run the whole `aide check` at 11-13 s apiece (`test_aide_check_no_errors`, `test_128` AC23, `test_134` AC7, `test_146` AC36 x2, `test_149` AC22, `test_150` AC4 x2, `test_159` AC7/8/9), where one session-scoped `run_checks` result would serve all of them, and several are the per-item "no new warning" shape already retired elsewhere on 2026-09-16. (b) about fifteen tests in `test_138`/`143`-`150`/`157` each regenerate the failure-mode specification or traceability artifacts from scratch at 7-27 s apiece (`test_147` AC24 is the slowest test in the suite at 27 s), where a session-scoped regeneration fixture, run twice once for the run-to-run comparison, would serve every byte-reproducibility assertion. Also slow and single: `test_126` AC12 (27 s) and `test_113` AC7's setup (22 s, a nested pytest collection). Nothing else exceeds 8 s, so the remainder is breadth, not outliers *(2026-09-18, engine 1.59.2)* → item 170
  - **2026-09-22** → routed to roadmap.md Stage 33 D0 (session-scoped aide check and specification-regeneration fixtures)
- [x] gap — item 162's Assumption A4 states that `UNUSED_OPERATOR_REASONS` entries are validated ("an operator name in that mapping that is not registered, or is registered and used, is itself a conflict the module reports rather than ignores"), but `_build_exercise` in `src/segfacet/traceability.py` only does `UNUSED_OPERATOR_REASONS.get(name, "")`, so a stale or contradictory entry is silently ignored. Nothing is wrong on this tree — the mapping ships empty and all 11 registered operators are used — and no AC or implementation step required the conflict report, so the assumption documents an intent the code does not carry. It bites the first item that adds an entry: a reason recorded for an operator that is registered and used, or for a name no longer registered, would read as a legitimate record rather than as the conflict A4 promises *(item 162, 2026-09-20, engine 1.59.2)* → item 172
  - **2026-09-22** → routed to roadmap.md Stage 33 D0 (UNUSED_OPERATOR_REASONS validated before its first entry lands)
- [ ] gap — `intensity_reference_delta` declares `modes=(16,)` with **zero** `signal`-role `ConsumedPath` entries (8 entries, all `bookkeeping` or `not-read`; measured 2026-09-20 over the live registry), and nothing reports it: `catalogue.path_classification_conflicts()` returns `()` on this tree because it refuses a non-empty `modes` beside an **empty** `consumed_paths`, not beside a consumed-path set carrying no signal. Item 148's own contract says a rule in that state "contributes no mode anywhere", so mode 16's second edge is claimed by a rule whose declaration attributes the claim to no path at all — the same blind spot one level down from the one item 148 closed. `border` is the benign twin (0 signal paths, but mode-less by declaration, so it claims nothing) *(item 164, 2026-09-20, engine 1.59.2)*
  - **2026-09-22** → routed to roadmap.md Stage 33 D3 (intensity_reference_delta's zero-signal claim on mode 16)
- [ ] gap — `traceability.bar_conditions`' condition 2 is universal where the roadmap's Stage 32 prose is existential, and the two readings only coincide for a mode with one corpus case. The roadmap asks that "a committed synthetic fixture expresses it, expected set names one of its own rules and agrees with the measured firing"; item 165's AC3 pins, and the implementation computes, `all(case_agrees(c) for c in mode.corpus_cases)` AND at least one `expected_firing` intersecting the mode's own `intended_rules`. So a fixture attached to a mode but not expressing it can hold condition 2 down: measured 2026-09-20, mode 1 carries `displace` (`expected_firing=('mislabel',)`, and `mislabel` is not among mode 1's intended rules) beside `fragment`, so `displace` plays no part in the "expressing" test yet its `case_agrees` is ANDed into the verdict. Nothing misfires today — every case agrees, and mode 4, this item's subject, has exactly one case — and the divergence is conservative, so it can only under-certify a mode, never certify one that is not at the bar. It matters because the checker was written once and generically for items 167, 168 and 169: the first consumer to run it over a mode with several corpus cases gets a false "not at the bar". Whether the bar means existential or universal is the roadmap's to say, not this checker's *(item 165, 2026-09-20, engine 1.59.2)*
  - **2026-09-22** → routed to roadmap.md Stage 33 D4 (condition 2 read existentially, as the roadmap words it)
- [ ] gap — `traceability.bar_conditions`' condition 2 is **rule-id-granular** while condition 4 is **detector-granular**, so a mode can hold condition 2 on a firing its own deciding detector never produced. Condition 2 tests `own_rule_ids.intersection(case.expected_firing)`, and `expected_firing` is a set of `rule_id`s (`failure_modes.measured_firing`), carrying no detector id — measured 2026-09-20, mode 3's `split` case fires `('fragmentation',)` through `fragmentation`'s `components` detector, which is mode 1's, and once item 167 adds a second `fragmentation` detector for mode 3 the expected set is unchanged, so condition 2 would read "met" even if the new detector fired on nothing. The evidence exists one level down — `Finding.detector_id` has carried the id since item 164 — so the checker could intersect on `(rule_id, detector_id)` pairs drawn from the measured findings rather than on rule ids drawn from the expected set. Item 167 closes the hole for its own mode with acceptance criteria asserting the `(fragmentation, neighbour_contact)` firing on the `split` case and on no other corpus case of either corpus, but the checker is generic and the next mode refined through an existing rule's new detector gets no such cover *(item 167, 2026-09-20, engine 1.59.2)*
  - **2026-09-22** → routed to roadmap.md Stage 33 D4 (condition 2 made detector-granular)
- [x] defect — a negative control whose "wrong" input is a **literal** silently stops being a control the moment live state drifts onto that literal, and it reports green rather than red: `tests/test_151_stage30_validation.py::test_adv_ac35_status_counts_parser_rejects_off_by_one` builds the clause `validated 7` and asserts it differs from the live derived count, which was 6 when written and became 7 the moment item 167 moved mode 3 to `validated` (measured 2026-09-20, validation round 1) — item 167 rebuilds that one from the live counts with a constructed `+1` perturbation, but its sibling `test_adv_ac35_status_counts_parser_rejects_wrong_n` (literal `15 modes` against a live 16) is the same shape, un-red only because the specification's mode count happens to be fixed, and a grep for the class across `tests/` has not been run *(item 167, 2026-09-20, engine 1.59.2)* → item 171
  - **2026-09-22** → routed to roadmap.md Stage 33 D0 (literal negative controls rebuilt from live values, class grepped)
- [x] defect — `scripts/aide_status_report.py`'s Synthetic Failure Corpus section still describes the pre-item-150 catalogue: `FAILURE_MODES_LEGEND` is a hand-typed dict of the eight vision-§6 modes (1 = "Label not aligned…", 8 = "Overlapping segments"), the prose says "each of the eight catalogued segmentation failure modes", the card renders `N/8`, and the footnote names "modes 1/4/8" as reconstructed-record — while the manifest rows it tabulates beside that legend carry the sixteen-mode numbering of `src/segfacet/failure_modes.py` (`split` = 3, `force_overlap` = 15), so the legend mislabels the table's own Mode column. The section already has the right model one screen down: the Feature Catalog section reads the committed `docs/aide/feature_catalogue.generated.json` with stdlib `json` only, and `docs/aide/failure_modes.generated.json` plus `docs/aide/traceability_matrix.generated.json` carry every mode's title, derived status, per-edge rung, corpus cases and the conformance table in the same import-clean form *(queue-022, 2026-09-21, engine 1.59.2)* → item 179
  - **2026-09-22** → routed to roadmap.md Stage 33 D1 (status report corpus section re-keyed off the generated specification)
- [ ] gap — no document states what each rule *decides*: `docs/aide/failure_modes.generated.md` and the traceability matrix say which mode a rule serves, and the decision logic (signal read, comparison, default threshold, detector tag) lives only in the ten module docstrings under `src/segfacet/heuristics/` plus `src/segfacet/default_config.yaml`. Every fact a reader needs is already machine-readable — `Rule.mode_declaration` carries the detector ids and their message tags, the config carries the defaults — so a `docs/aide/rules.generated.md` (one row per detector: question asked, path read, fires-when, default, modes served) is a small generator beside `failure_modes.py`'s, not prose to maintain *(queue-022 review, 2026-09-22, engine 1.59.2)*
  - **2026-09-22** → routed to roadmap.md Stage 33 D4 (generated docs/aide/rules.generated.md)
- [x] gap — maintainer decision on the geometric corpus base (2026-09-22): replace `clean_gt`'s five axis-aligned boxes with a **lordotic** L1–L5 — inter-body gap 8 mm (a disc height, was 15), each body rotated about the L-R axis by a per-level sagittal tilt (−8°, 0°, +8°, +18°, +35° for L1…L5, positive = anterior edge lower), the A-P centroid path the integral of those tilts so the tilt is the curve's tangent (L1 slightly posterior of L2, L5 ~26 mm posterior), and **no lateral hump** (scoliosis is not the base case). A scratchpad prototype of exactly this passes `run_qc` under the bundled default config with zero findings: one component per label, nearest-other-label 7–9 mm, spline offsets ≤ 0.34 mm, spacings 32.7–36.8 mm, volumes 18.7–19.4 k mm³, extents ≤ 34 mm. Every operator that cuts along array axis 2 (`fragment`, `split`) assumes axis-aligned bodies and is re-authored on the new base, and every measured value the specs quote moves *(queue-022 review, 2026-09-22, engine 1.59.2)* → item 173
  - **2026-09-22** → routed to roadmap.md Stage 33 D1 (the lordotic base)
- [x] gap — maintainer decision (2026-09-22) on the `displace` case: the displacement should be mostly left-right with the anterior-posterior component reduced (today it is 13 voxels along both non-stacking axes) *(queue-022 review, 2026-09-22, engine 1.59.2)* → item 177
  - **2026-09-22** → routed to roadmap.md Stage 33 D2 (displace mostly left-right)
- [x] gap — maintainer decision (2026-09-22) on `crop_at_border`: the operator today *translates* one vertebra 5 voxels beyond the anterior face and clips the overhang (the contact sheet showed exactly that, a displaced L3 — not a display error), which is a displacement, not a field-of-view cut. Replace it with a crop of the clean-control **volume** itself so the foreground is cut by the image face (A-P or S-I), preferably removing a large part of L5, so a truncated vertebra's volume/extent genuinely differ from a complete one's and the border condition's gating of the size rules (`bounds`, `reference_delta`) has a fixture to be measured on *(queue-022 review, 2026-09-22, engine 1.59.2)* → item 175
  - **2026-09-22** → routed to roadmap.md Stage 33 D2 (crop_at_border as a volume crop)
- [x] gap — maintainer decision (2026-09-22) on `fuse_adjacent`: the operator today relabels L4 as L3 and leaves the 15 mm gap, so the fused label is two disconnected components and the sequence has a hole (which is why `fragmentation` and `coverage` co-detect it). A fused vertebra segment is one connected label over two bodies with the caudal labels renumbered so the sequence stays continuous: bridge the gap between L3 and L4 under label L3, then shift L5 → L4 *(queue-022 review, 2026-09-22, engine 1.59.2)* → item 176
  - **2026-09-22** → routed to roadmap.md Stage 33 D2 (fuse_adjacent bridged and renumbered)
- [ ] gap — maintainer decision (2026-09-22) on `force_overlap` / the `overlap` rule / mode 15: a single-channel label map cannot hold an overlap, so the committed fixture is a label map with L1 shifted 18 voxels caudally, whose overlap exists only in a reconstructed two-channel record (`detection="reconstructed_record"`) — the fixture does not express the mode it is attributed to. Mode 15 may stay unimplemented, but fixtures and rules must be accurate or dropped: either the rule is declared to need a multi-channel input and the fixture is retired, or both go *(queue-022 review, 2026-09-22, engine 1.59.2)*
  - **2026-09-22** → routed to roadmap.md Stage 33 D2 (force_overlap and overlap: declared multi-channel, or retired)
- [x] gap — maintainer decision (2026-09-22) on mode 3 (split vertebra segment) and its fixtures: the split-off part need not carry a neighbour's label — it may carry its **own** label — so the mode has two sub-types, (a) the part labelled as part of a neighbouring vertebra, which is mode 2's counterpart (one side: a label fuses neighbouring vertebrae; other side: a label covers only the split part of a vertebra), and (b) the part labelled independently. Two cases on the lordotic base: `split` re-authored as relabelling a small caudal portion of L4 (voxels below an S-I level) as L5; and a new `split_own_label` where that portion gets an independent label and the labels cranial to it shift up (L5 kept, the split part becomes L4, the remaining L4 becomes L3, and so on upward) *(queue-022 review, 2026-09-22, engine 1.59.2)* → item 174
  - **2026-09-22** → routed to roadmap.md Stage 33 D2 (split at ~20 % plus split_own_label)
- [x] gap — maintainer decision (2026-09-22) on the `neighbour_contact` detector: `fragmentation`'s `components`/`islands` detectors read one label in several parts, which is not mode 3 (one vertebra under several labels), so `fragmentation` should not declare mode 3; `neighbour_contact` *is* mode 3's signal — and beyond it, identifies islands in contact with another label as merge candidates — so it moves out of `fragmentation` into a rule of its own. The cost is the one the item-168 brief priced: `len(iter_rules()) == 10` pinned twice, rule-id enumerations across fourteen test modules, and a corpus regeneration — paid once, in the same queue as the base regeneration *(queue-022 review, 2026-09-22, engine 1.59.2)* → item 187
  - **2026-09-22** → routed to roadmap.md Stage 33 D3 (neighbour_contact as its own rule)
- [x] gap — maintainer decision (2026-09-22) on `coverage`: it detects a missing **label** in the sequence (`relationships.missing_levels`), not a missing segmentation — it cannot see whether a vertebra is present — so it is re-homed from mode 6 (vertebra not segmented) to mode 8's family, specifically mode 10 (skipped level label), which today is `proposed` with no rule. Mode 6 then loses its only synthetic-demonstrable edge and its `remove_level` case becomes a co-detection *(queue-022 review, 2026-09-22, engine 1.59.2)* → item 188
  - **2026-09-22** → routed to roadmap.md Stage 33 D3 (coverage re-homed to mode 10)
- [x] gap — maintainer decision (2026-09-22) on `mislabel`: the rule is about label consistency — keep the `ordering` detector, extend it to any out-of-sequence label, and treat the spline offset as unrelated: the `spline_offset` detector must not fire on a displaced segment, so it is retired from `mislabel` (or re-homed as an informational anatomy signal). Consequence to author explicitly: `displace` is then detected by nothing in the label map (`bounds`/`reference_delta` need a reference), so its expected firing set becomes empty unless a new alignment signal is specified *(queue-022 review, 2026-09-22, engine 1.59.2)* → item 189
  - **2026-09-22** → routed to roadmap.md Stage 33 D3 (mislabel keeps ordering; spline_offset leaves)
- [ ] gap — maintainer decision (2026-09-22) on `bounds`: (1) the x/y/z extents are scan-axis-aligned and therefore coarse for a tilted vertebra — on the lordotic prototype L5's 25 mm body measures a 34 mm A-P and S-I extent — so extent bounds need a vertebra-local coordinate system before they mean anything; (2) `bounds` must be suppressed for a label carrying a border-touch flag (a truncated vertebra is small by construction) — verified 2026-09-22 that `heuristics/bounds.py` reads no `touches_*` flag today. The same suppression applies to `reference_delta`'s size features *(queue-022 review, 2026-09-22, engine 1.59.2)*
  - **2026-09-22** → routed to roadmap.md Stage 33 D3 for the border-touch suppression; the vertebra-local extents half belongs to Stage 27 (maintainer, 2026-09-22)
- [ ] gap — maintainer decision (2026-09-22) on `sequence`: an out-of-sequence label can express any of modes 8–11, so the rule serves that family and needs sub-type differentiation in what it reports (a swap, a skip, a transitional label, a shift); mode 12 (shifted sequence) cannot be identified without external context and stays out *(queue-022 review, 2026-09-22, engine 1.59.2)*
  - **2026-09-22** → routed to roadmap.md Stage 33 D3 (sequence sub-type reporting across modes 8-11)
- [ ] gap — maintainer decision (2026-09-22) on presentation and naming: mode 1 is the catch-all accuracy mode, so a case (or a mode → rule edge) is attributed to it only when no sub-mode or other mode applies, and the rendering should say so rather than list mode 1 beside them; and the vocabulary must be applied distinctly — modes describe the **vertebra** (mode 2 "fused vertebra segments" = one label covers parts of several vertebrae; mode 3 "split vertebra segment" = one vertebra covered by more than one label), while "fragmentation" is one label in several parts, which is neither *(queue-022 review, 2026-09-22, engine 1.59.2)*
  - **2026-09-22** → routed to roadmap.md Stage 33 D3 (mode 1 as catch-all, vocabulary)
- [ ] gap — maintainer decision (2026-09-22) on the displaced-vertebra signal: a vertebra off the spinal curve is worth checking but aligns with no failure mode, so it becomes a second entry in `failure_modes.CONDITIONS` beside `fov_truncation` (a property of the anatomy or the case that other rules read, not a defect of the segmentation), with `mislabel`'s `spline_offset` detector re-homed to record that condition and the `displace` corpus case re-attributed as that condition's fixture, the way `crop_at_border` is `fov_truncation`'s *(queue-022 review, 2026-09-22, engine 1.59.2)*
  - **2026-09-22** → routed to roadmap.md Stage 33 D3 (displaced-vertebra condition)
- [ ] gap — maintainer decision (2026-09-22) on mode 6 (vertebra not segmented) once `coverage` leaves it: its label-map signal is the inter-centroid distance measured along the fitted spine curve — a missing vertebra doubles one spacing whether or not the labels were renumbered — so mode 6 needs a rule over `stage3.spacing_consistency.spacings_mm[]` (extracted since Stage 3, read by no rule), which would also be the first detector for the renumbered form `remove_level_relabel` has carried undetected since item 143. The split-slab fraction for the re-authored `split` case is ~20 % of the body, not the current 40 % *(queue-022 review, 2026-09-22, engine 1.59.2)*
  - **2026-09-22** → routed to roadmap.md Stage 33 D2 for the ~20 % split fraction; the mode-6 spacing rule is not in Stage 33 (target modes 3 and 4 only, maintainer 2026-09-22) and stays open for a later per-mode queue
- [x] defect — `coverage`'s always-on `missing_interior` detector reports **T13 absent** on any label map holding both T12 and L1, because `relationships.missing_levels` walks `segfacet.labels.CANONICAL_ORDER`, where the transitional T13 sits between T12 and L1 (and L6 between L5 and S1), and treats every skipped entry as an interior gap. Measured 2026-09-22 on a prototype own-label split case (labels T12, L1–L5 present, all six contiguous bodies): `missing: ['T13']`, one `Missing interior level(s): T13` finding, verdict flagged-for-review. `synth/clean_gt.py`'s docstring calls this the "transitional-vertebra trap" and guards the *generator* against crossing the T12→L1 or L5→S junction, but nothing guards the rule: a real thoraco-lumbar scan, which is the common case, is flagged for an anatomy it does not have. A transitional level should count as missing only when the case is configured to expect it (the mode-11 flag the item-150 sign-off asked for), never by default *(queue-022 review, 2026-09-22, engine 1.59.2)* → item 186
  - 2026-09-22: maintainer decision — fix it in the canonical order itself: `segfacet.labels.CANONICAL_ORDER` must admit every numbering variation (T12→L1 with or without T13, L5→S1 with or without L6) as a continuous sequence, so no rule reading it sees an absent transitional vertebra as a gap.
  - **2026-09-22** → routed to roadmap.md Stage 33 D3 (CANONICAL_ORDER admits transitional variants)
- [ ] gap — maintainer decision (2026-09-22) on the fused case: a bridged, renumbered `fuse` fires nothing under the shipped rules (measured 2026-09-22 on the lordotic prototype: one connected label of 45 k mm³, continuous sequence, verdict pass), so mode 2 (fused vertebra segments) must also be served by the inter-centroid-distance rule mode 6 needs — a fused label's centroid sits between two bodies, so both spacings to its neighbours read ~1.5× the pitch — and `bounds`' 8 000–120 000 mm³ lumbar tolerance is noted as far too wide to be a proxy (a 65 %-cropped L5 at 7 719 mm³ only just trips it; refined later, no action now). The 65 % inferior FOV cut is the settled `crop` depth: L5 remnant 7 719 mm³, `touches_inferior`, `bounds` fires on volume, `border` suppresses the expected end — the fixture the border→size-rule gating is measured on *(queue-022 review, 2026-09-22, engine 1.59.2)*
  - **2026-09-22** → routed to roadmap.md Stage 33 D2 for the 65 % crop depth; the mode-2 spacing rule and the bounds tolerance are not in Stage 33 (target modes 3 and 4 only, maintainer 2026-09-22)
- [x] automation — the prototype code behind the 2026-09-22 review decisions is committed as provenance under `scripts/prototypes/2026-09-22-lordotic-corpus/` (the lordotic base builder, the five re-authored operators, the pipeline check, and the two contact-sheet renderers; its README records what each measured). It is imported by nothing and tested by nothing, and is **to be absorbed, not kept**: the items that implement the base, the operators and the corpus rendering take what they need into `src/segfacet/synth/` (and the rendering into wherever the status report's corpus section is rebuilt), and the last of them deletes the directory. A prototype left beside the shipped code becomes a second, drifting truth *(queue-022 review, 2026-09-22, engine 1.59.2)* → items 173-178 (178 deletes the directory)
  - **2026-09-22** → routed to roadmap.md Stage 33 D1 (prototype absorbed into src/segfacet/synth/, then deleted)
- [ ] gap — Stage 32 criterion 1 (at least one mode fully specified end to end) does not hold, live-recomputed 2026-09-22: modes 3 (split) and 4 (islands) both meet bar conditions 1-5 (`traceability.bar_conditions`) but carry `MODE_SIGN_OFFS` records at `outcome="intermediate-state"`, not `"at-the-bar"` (gate 7, resolved 2026-09-22), so condition 6 fails for both and the set of modes at the bar is empty. The 2026-09-22 maintainer review (the entries above, dated the same day) names what must change before either mode clears the bar; that remedial work is queue 023's, not this item's *(item 169, 2026-09-22, engine 1.59.2)*
  - **2026-09-22** → routed to roadmap.md Stage 33 D5 and D6 (modes 3 and 4 re-signed at the bar; Stage 33's close attests Stage 32 criterion 1)
- [ ] gap — item 166's `split` operator carries no entry in `segfacet.eval.severity_ladder`'s `RECORDED_MARGINS` / `KNOWN_CROSS_MODE_COUPLINGS` (confirmed live 2026-09-22: `run_severity_harness()`'s `per_ladder` has no `split` key). Item 154 measured that constant set on 2026-09-16, before item 166 added `split`; widening the ladder set to cover it is a measurement item, not this validation item's to fix *(item 169, 2026-09-22, engine 1.59.2)*
  - **2026-09-22** → routed to roadmap.md Stage 33 D4 (severity-ladder constants re-measured, covering the split operators)
- [x] defect — `src/segfacet/default_config.yaml` line ~140 still quotes the split case's neighbour-contact value as 750.0 mm^2; item 173's lordotic base re-measures it at 775.0 mm^2, and the file is outside item 173's Authorised paths *(item 173, 2026-09-23, engine 2.1.0)* → item 187
- [ ] knowledge — maintainer position (2026-09-23): FACET has no full multi-channel segmentation support, so an overlap fixture is not needed. On item 173's lordotic base `ForceOverlapPerturbation`'s S-axis bounding-box shift overlaps pairs 20→21 and 21→22 only (0 voxels for 22→23 and 23→24), so item 173 left the operator unfixed and moved `test_038`'s unspecified-target `force_overlap` case onto an L1–L3 span. Candidate follow-up: remove the `force_overlap` operator and corpus case, its `severity_ladder` entries and its mode-15 `failure_modes` entries *(item 173, 2026-09-23, engine 2.1.0)*
- [ ] defect — `src/segfacet/heuristics/mislabel.py`'s module docstring still quotes the box-base corpus offset margins 2.510990 (interior ceiling), 17.507445 (crop_at_border) and 18.718604 (displace) mm, and `tests/test_123_recalibrate_and_regenerate.py` pins those literals in the docstring; item 173's lordotic base re-measures them at 5.624555, 18.025609 and 17.615126 mm, and mislabel.py is outside item 173's Authorised paths *(item 173, 2026-09-23, engine 2.1.0)*
- [ ] knowledge — on item 173's lordotic base the `relabel_swap` case's `stage3.curvature.coronal_tangent_angles_deg` reads 0 and -179.99999999999997 (and `coronal_curvature_deg` 180.0) purely from the sign of ~1e-16 L-R residue in `atan2`; `tests/test_131_tangent_direction_normalisation.py`'s `_PRE_ITEM_OTHER_CURVATURE_FIELDS` pins that row at abs=1e-6, so a platform whose residue sign flips would read +180 and fail it *(item 173, 2026-09-23, engine 2.1.0)*
  - 2026-09-23: the test half was taken into item 173's scope (its second `## Correction — 2026-09-23 (review findings)`, part 2: circle comparison, three residue-set fields skipped). The production half remains open: when the two residues' signs differ, `features/orientation.py`'s unwrapped coronal array reads `[0, ±180, ±180, ±360, ±360]`, so `coronal_curvature_deg` becomes 360, above the sagittal 348.91102, and `total_curvature_deg` becomes 360 with `curvature_plane` `"coronal"`. A tangent on `atan2`'s branch cut decides a reported feature.
- [x] defect — `segfacet evaluate` does not catch a `FacetInputError` raised inside `evaluate_cohort` (`src/segfacet/cli.py` `_handle_evaluate` calls it outside every `try`), so a cohort pairing a candidate and a GT of different array shapes ends in a traceback from `eval.overlap.compute_overlap` instead of an error message and exit 1 *(item 175, 2026-09-24, engine 2.1.0)* → item 180
- [x] defect — `tests/test_049_acceptance_stage6.py::test_ac11_size_distorting_perturbation_flags_label_22_out_of_range` and its `..._fires_reference_delta_finding_on_label_22` sibling are vacuous on the lordotic base: measured 2026-09-24, the unperturbed `clean_control` fixture already reads `out_of_range_features == ['physical_volume_mm3']` on label 22 against `bundled_default_reference()` and fires `reference_delta` on labels 20-23, so both tests pass whether or not the perturbation distorts anything *(item 175, 2026-09-24, engine 2.1.0)* → item 181
- [x] defect — `tests/test_090_reference_derived_defaults.py::test_ac14_mode6_crop_at_border_fires_bounds_on_label_22_against_verse_v1` is vacuous: measured 2026-09-24, `run_qc_with_reference` against `bundled_production_reference()` fires `bounds` on every label (20-24) of the unperturbed `clean_control` fixture, so the test cannot tell a crop from no crop *(item 175, 2026-09-24, engine 2.1.0)* → item 181
- [x] gap — `catalogue._scan_synth_rule_mode_map` (the "corpus-derived" rule→mode map behind the catalogue's `rule_mode_map` evidence and `rule_declaration_conflicts`) reads literal `Expectation(failure_mode=…, expected_rule_ids=frozenset({…}))` calls in `src/segfacet/synth/*.py`, not the committed manifests' cases, so an operator (or operator branch) no corpus case applies still designates rule→mode pairs, and a non-literal designation silently drops out; measured 2026-09-24, keeping `fuse`'s unbridged literal while `fuse_adjacent` expects `()` yields two false conflicts *(item 176, 2026-09-24, engine 2.1.0)* → item 182
- [x] defect — minor: `FusePerturbation(bridged=True)` (`src/segfacet/synth/component_shape.py`, bridged branch of `apply`) decides which label is "low" once for the whole array from the two labels' mean stacking-axis index, then per column fills `max(low-label index)+1 .. min(high-label index)`; in a column where the two labels sit in the locally reversed order, or where one label is split into runs either side of the other, that range is empty and a real background gap between them is silently left unfilled, contrary to item 176's AC4 wording; dormant on the committed base (all 682 bridged columns of `build_clean_spine()` are in global order, measured 2026-09-24), and `tests/test_176_fuse_bridged.py`'s helpers share the same global-direction assumption so no test would catch it *(item 176, 2026-09-24, engine 2.1.0)* → item 183
- [ ] framework — `aide check`'s "listed under both May change and Asserts against" lint (`item_spec_warnings`) compares the two lists by exact string only, so an Asserts-against glob that matches a May-change path in the same spec (item 178 wrote `tests/test_*.py`, meaning "every other test", beside its own `tests/test_178_corpus_sheet.py`) passes `aide check` silently and surfaces only as a deterministic `aide scope` FAIL on the claim branch; running `path_matches` of each may_change path against each asserts_against pattern would catch it at spec time *(item 178, 2026-09-24, engine 2.1.0)*
